# 设计规范

`[共同]` 本文是当前Web和原生微信小程序唯一权威设计规范。`design-system/70/` 仅保存早期设计工具输出，不得覆盖本文或当前 `apps/web/`、`apps/miniprogram/` 实现。规范与当前界面冲突时先修正规范；未经用户明确确认，不得以规范、审查或无障碍建议为由擅自改动现有界面。

## 平台适用标记

- `[共同]` 同时约束Web和小程序。
- `[Web]` 只约束响应式Web及微信内置浏览器中的Web页面。
- `[小程序]` 只约束原生微信小程序。
- `[候选]` 只记录待单独批准方向，不是当前验收要求。

`[共同]` 所有现行设计条款必须显式标记或置于已标明平台的子节，不再把未标条目自动套用到另一平台。平台显示文案以 `PRODUCT.md`“品牌命名”矩阵为准；矩阵中的Web/小程序差异允许保留，未经确认不得为“统一”而改页面。

## 方向 `[共同]`

整体采用克制的 iOS-inspired 产品界面：安静、专业、触感明确、便于扫描。它仍是响应式数据工具，不伪装成原生iPhone应用，也不采用房地产营销页风格。

- 浅灰分组背景、白色内容层、清晰分隔线和克制阴影。
- 系统字体栈：`-apple-system, BlinkMacSystemFont, "SF Pro Text", "PingFang SC", "Segoe UI", sans-serif`。
- 不远程分发未授权的SF Pro字体。
- 固定字号层级，不按视口缩放字体；字间距为0。
- 数值使用 `font-variant-numeric: tabular-nums`。
- iOS感来自排版、留白、分组、分段控件和按下反馈，不依赖大面积毛玻璃或渐变。
- 唯一识别性元素优先围绕0%基准线、月度时间轴或多城市比较设计。

## 颜色令牌 `[共同]`

```text
页面背景      #F2F2F7
内容背景      #FFFFFF
次级内容背景  #F7F7FA
主文字        #1C1C1E
次文字        #5F6368
分隔线        rgba(60, 60, 67, 0.18)
交互强调      #007AFF
交互文字      #005FCC
新房          #176C79
二手房        #D07A34
二手房文字    #92501E
上涨          #C94F45
上涨文字      #B23A32
下跌          #167D67
下跌文字      #0F6F5D
错误文字      #B23A32
零轴          #8E8E93
焦点环        #005FCC
```

组件只能使用语义令牌，不能散落硬编码颜色。`交互强调`和图表数据色用于控件背景、图形或大号文字；小号文字、链接和标签必须使用对应的文字令牌。所有文字令牌在实际背景上达到至少4.5:1，图形和粗线达到至少3:1。`packages/design-tokens/` 维护平台无关源令牌，并为Web CSS变量和小程序样式生成平台产物。

布局使用4像素基础尺度：`4 / 8 / 12 / 16 / 24 / 32 / 48 / 64px`。浮层层级使用语义令牌，顺序为内容、焦点、粘性导航、Popover、遮罩、Sheet、Toast和跳转链接，不在组件中散落任意 `z-index`。

## 组件与交互

- `[共同]` 环比/同比、新房/二手房使用分段控件；界面统一使用短标签，产品范围、数据说明和来源审计继续使用“新建商品住宅/二手住宅”官方术语。
- `[共同]` 时间范围和户型使用菜单或选择器。
- `[共同]` 70城选择器必须支持中文、完整拼音和拼音首字母搜索；常用六城置顶，其余城市按拼音首字母分组。
- `[共同]` 城市组和组内选项按完整拼音升序排列；列表行只显示中文城市名，不重复展示拼音。
- `[共同]` 已选城市置灰并标记“已选”，达到三城上限后禁用未选城市；允许清空主图，空状态提供“添加城市”和恢复默认城市操作。
- `[共同]` 当前住宅类型、指标和面积段集中在粘性数据导航中显示一组全局口径标签；对应平台的三个数据分区不重复标签，具体显示名以 `PRODUCT.md`“品牌命名”矩阵为准。“按变动率排序”“最新月份”“本站计算”等信息作为次级说明单独呈现。筛选分段控件属于操作入口，不套用只读标签样式。
- `[共同]` 粘性数据导航的三个锚点必须为固定顶栏和导航自身预留滚动偏移；点击后对应分区标题完整露出，并与导航底部保持至少8像素间距。
- `[共同]` 市场涨跌遵循中国大陆常用表达：上涨红色、下跌绿色；错误和过期状态使用独立错误令牌，不复用涨跌语义。
- `[共同]` 涨跌同时显示正负号或方向图标，不只依赖颜色。
- `[共同]` Web控件触控区域不小于44×44像素，触控目标间距至少8像素。小程序当前可见图标和按钮尺寸保持现状；其中未达到44×44像素的项目登记为待确认的无障碍问题，不构成本规范自动授权的界面修改。后续获得单独确认时，优先扩大不可见命中区并保持现有可见尺寸和位置。
- `[共同]` 分段控件尺寸稳定，选中状态不能只靠颜色，切换不产生位移。
- `[Web]` Web手机复杂选择使用底部Sheet，Web桌面使用Popover或菜单，共享业务状态。
- `[Web]` 城市Sheet打开后聚焦搜索框；Web移动端限制焦点和页面滚动，关闭后焦点返回“添加城市”按钮。城市列表使用原生按钮语义，不伪装成未实现方向键模型的listbox。
- `[小程序]` 当前城市切换使用原生Picker，趋势城市使用当前底部选择面板和触摸搜索；不得用Web焦点圈定、Popover或键盘模型作为小程序当前验收要求。
- `[共同]` 按下反馈在100毫秒内出现；常规过渡150-250毫秒，使用克制ease-out。
- `[Web]` 仅在粘性导航、Popover或Sheet需要区分背景时使用轻度 `backdrop-filter`，并提供不透明回退。
- `[共同]` 卡片圆角不超过8像素；不使用嵌套卡片、装饰性渐变和悬浮大卡片布局。
- `[共同]` Web新增或替换的图标使用Lucide。小程序当前图标、符号及其可见尺寸属于现有界面基线，未经单独确认不得替换；后续新增图标优先使用本地资源或语义一致的矢量图标，不强制复用Web运行时包。
- `[共同]` 组件覆盖适用的default、hover、focus-visible、active、selected、disabled、loading和error状态。
- `[共同]` 新增设计不使用Emoji、手绘SVG、弹跳动效、装饰性入场编排或仅悬停可见的关键操作；该规则不授权自动替换小程序现有图标或符号。
- `[共同]` 空数据状态必须说明当前筛选没有可用记录，并提供清除筛选、返回默认范围或查看来源的动作；不得只显示空白图表。
- `[共同]` 加载超过300毫秒使用稳定尺寸的骨架状态；失败状态说明原因并提供重试，不把网络错误伪装成无数据。

## 市场位置组件

- `[Web]` Web的70城温度、同级城市对比和省内城市对比组成开放式分析区，不拆成三张悬浮卡片；桌面三栏、平板两栏、手机单列。
- `[小程序]` 小程序当前先显示全宽70城温度及温度走势，再把70城排名、同级城市对比和省内城市对比保持在同一行三列；这是已确认的紧凑平台布局，不得套用Web手机单列规则改回单列。
- `[Web]` Web在首屏使用选择器切换“重点城市”，不提供自动定位；重点城市写入URL的 `focus` 参数并可独立于趋势城市。趋势列表不足三座时自动加入新重点城市，已满时保持趋势列表不变；复制链接和刷新后恢复。
- `[小程序]` 小程序在标题行右侧使用定位图标和原生选择器显示“定位城市/当前城市”，“70城温度”分析区不重复放置重点城市或趋势城市快捷按钮。分享路由中的 `focus` 优先；自动定位成功后趋势列表重置为定位城市，手动选择时仅在不足三城时追加。
- `[共同]` 70城温度内部保存上涨、持平、下跌和缺失四类，四类合计为70。正常页面只显示上涨、持平和下跌三项，分布条以有效城市数为分母；缺失数大于0时必须显示异常说明。缺失值不绘制、不出现在正常Tooltip和月份摘要中，不计入排名或平均值，也不得归入持平。
- `[共同]` 温度模块下方展示同一口径的月度历史分布；面积筛选使用平台原生选择控件，四档宽度统一，避免在手机端挤压分段控件。
- `[Web]` Web同级对比只展示重点城市附近最多5个名次，同时显示同级总数、名次和本站简单平均。
- `[小程序]` 小程序的70城排名、同级城市对比和省内城市对比均使用固定高度的完整可滚动排名列表，并把当前城市滚动到可见范围；不得按旧Web规则截成附近5城。
- `[共同]` 省内对比完整显示70城样本中的同省城市，并持续展示“仅比较70城名单中的同省城市”说明；单城市省级区域使用不占整列高度的紧凑解释状态。
- `[共同]` 排名行使用等宽数字、明确正负号和“当前”标签；不得使用火焰、温度计、投资热度或红绿灯式评分。
- `[共同]` 70城、同级和省内列表使用竞赛排名：同值显示相同名次，后续名次跳号，例如 `1、1、3`；三个范围分别重排，`null`不显示排名，分母只统计有效城市。同值行按 `city_id` 稳定排列，“当前”标签与城市名保持同一行。
- `[共同]` 常用六城卡片数字只表示六城内部排序。当前Web同值仍显示连续位置号，小程序同值显示竞赛名次；不得把这两个数字解释为市场位置排名，也不得在未获确认时为统一形式而改动页面。
- `[共同]` 新房/二手房和环比/同比沿用页面全局筛选，不在分析区重复增加一套口径控件。

## 图表

- `[共同]` 百分比纵轴，0%基准线明显但不抢夺数据注意力；最多三条主趋势线。
- `[共同]` 图例靠近图表并支持显隐；城市系列颜色使用独立 `series-1/2/3` 令牌，不复用新房/二手房语义色。
- `[共同]` 时间轴使用本地化月份格式，窄屏自动减少刻度；缺失值断线，加载失败显示原因与重试入口。
- `[共同]` 颜色之外使用标签、线型、符号或文字辅助区分。
- `[共同]` 累计变化图紧接主趋势图，复用相同城市颜色和线型；显示首月基准、最新累计值及相对起点涨跌。文案必须明确“起点100为比较基准，不是实际元/㎡房价”。
- `[Web]` 拖动时间条和缩放按钮共享同一窗口状态，并持续显示当前可见月份范围；图例至少保留一条可见趋势线。
- `[Web]` Tooltip可通过鼠标和触摸访问且不改变容器高度；键盘用户通过筛选器、月份步进器或可访问数据表获得等价信息，不要求逐个聚焦Canvas数据点。
- `[Web]` 提供文字摘要或可访问数据表入口；精确数据入口命名“查看精确数据”，桌面使用语义表格，Web手机使用月份选择和按城市分组的键值列表。
- `[小程序]` 当前wx-f2图表以触摸滑动、图例点击、日期/数值读数和错误占位为验收范围；当前页面不显示精确数据入口，也不要求Web缩放按钮、月份步进器或键盘等价表格。
- `[Web]` 图表动效尊重 `prefers-reduced-motion`，数据默认即使无动画也可见。

## Web响应式

本节全部为 `[Web]`，不约束小程序当前四列筛选、三列排名或原生Canvas布局。

```text
手机端       360-767px
平板端       768-1023px
电脑端       1024px及以上
内容最大宽度 1440px
```

桌面端：

- 核心筛选尽量保持在一行，空间不足时按组换行。
- 六城概览使用三列或六列自适应布局。
- 主趋势图高度不低于360像素。
- 长表格表头保持可见，表格自身滚动。

手机端：

- 顶部只保留产品名、统计月份和必要菜单。
- 首页主标题使用“读懂你的城市住宅价格变化”，以用户关心的城市为叙述起点，同时坚持“住宅价格指数”口径；说明文字占满可用内容宽度，并明确不推算成交单价。
- 统计月份只在顶部显示一次；官方发布日期放在页面说明下方，不再使用右侧重复日期块。
- 指标与住宅类型在同一筛选行，时间范围与“恢复默认筛选”在下一行；两列严格等宽，因此指标、住宅类型和时间范围三组宽度一致，所有控件继续保持至少44像素触控高度。
- 六城概览的排序口径放在标题下方左对齐，辅助状态和指数文字不小于12像素。
- 使用 `env(safe-area-inset-top)` 和 `env(safe-area-inset-bottom)` 处理安全区。
- 仅在需要安全区时使用 `viewport-fit=cover`，并确保非iOS浏览器有正常回退。
- 六城概览使用两列或单列，不等比例缩小桌面卡片。
- 图表高度260-320像素，支持触摸Tooltip和横向拖动。
- 图表横向拖动只在绘图区内生效，不拦截浏览器滚动、微信返回或iOS边缘返回手势；必须提供缩放控件作为手势替代。
- 精确数据在手机端不使用横向表格，改为按城市分组的双列键值列表；页面不能横向溢出。
- 输入框和选择器正文不小于16像素，避免iOS Safari聚焦放大。
- 不禁用页面缩放，不使用 `user-scalable=no`。
- 本地保存的筛选状态必须带版本号，可迁移或安全清除；提供“清除本地保存”入口，不把本地状态当作账户数据。

桌面端趋势比较使用开放式全宽分析区，不用巨型白色圆角外框包裹主图、累计图和数据表；三部分通过留白与分隔线建立层级。完整来源与数据状态集中在来源区，页脚只保留内部环境标识和必要入口，避免重复口径文本。

## 小程序当前布局

本节全部为 `[小程序]`，以 `apps/miniprogram/` 当前页面为事实基线：

- 顶部显示“全国城市观察”和数据截止月份，标题行右侧保留定位图标、当前城市和原生选择器。
- 新房/二手房、环比/同比、面积和时间范围保持同一行四列；不得套用Web手机两行筛选要求。
- 70城温度和温度走势先全宽显示，70城排名、同级城市对比和省内城市对比随后保持同一行三列并各自滚动。
- 趋势区使用当前图例、紧凑“添加城市”按钮、wx-f2主图和累计变化图；不因Web拥有缩放控件或精确数据表而新增小程序控件。
- 触摸、横竖屏、Canvas清晰度、列表完整滚动、文字不重叠和页面无水平溢出必须在开发者工具及Android/iPhone真机分别验证。

## 无障碍

- `[共同]` 筛选器有关联名称并暴露适用的selected、expanded、disabled等状态。
- `[共同]` 文本和关键图形满足适用平台的可读性要求；普通文本至少4.5:1，大文本至少3:1，图表线条和控件图形至少3:1。
- `[Web]` 键盘焦点顺序与视觉顺序一致，`focus-visible` 清晰；Tooltip不是关键信息的唯一来源，并提供键盘等价入口。
- `[小程序]` 当前以触摸、平台无障碍名称、朗读顺序和真机放大文字不截断为验收范围；Web键盘焦点规则不直接套用。
- `[共同]` 图标按钮有可访问名称，装饰图标不重复朗读。
- `[Web]` 尊重 `prefers-reduced-motion`，放大文本后不截断核心内容。

## 性能与兼容

- `[Web]` 支持验收时Chrome、Edge、Safari当前稳定版及前一个稳定版；微信内置浏览器至少验证一台Android和一台iPhone。
- `[共同]` 发布数据包含完整70城和四档官方面积口径；首屏默认只渲染常用六城概览和当前趋势选择，不预渲染70城卡片。
- `[Web]` 首屏JavaScript与CSS gzip总量目标不超过500 KB；超出时记录原因并优先按需加载ECharts组件。
- `[Web]` Lighthouse Accessibility目标不低于90，但不能替代键盘、触摸和真机检查。
- `[小程序]` 以微信开发者工具编译、包体积记录、Android/iPhone Canvas清晰度、触摸响应、前后台恢复和横竖屏为当前兼容证据；Web浏览器或Lighthouse结果不能替代。

## UI Skill 使用 `[共同]`

- 新页面或重构：用 `frontend-design` 明确信息层级，再用 `ui-ux-pro-max` 查询设计系统、图表和UX建议。
- 打磨：用 `impeccable` 的product/dashboard规范检查密度、一致性、状态和视觉层级。
- `[Web]` 发布审查：用 `web-design-guidelines` 获取最新规则，记录获取日期或版本及 `file:line` 结果。
- `[小程序]` 发布审查：以当前不可变候选、微信开发者工具编译和Android/iPhone验收为依据；Web规则或截图不能替代小程序证据，也不得借审查擅自改动页面。
- 小范围修复只调用直接相关skill，不重复执行完整设计流程。

Skill推荐只作候选。豪宅营销、超大Hero、衬线展示字体、强CTA或大面积毛玻璃均不适用于本产品。

### 历史证据：本轮内部预览设计记录

以下记录仅证明标注日期的设计工具执行和当时的候选方向，不构成当前页面、当前候选构件、无障碍、真机或发布验收证据。当前验收必须绑定当前源码/候选身份和适用平台证据；`design-system/70/` 输出也不能覆盖本规范。证据日期为2026-07-16和2026-07-18，相关工具输出文件为`design-system/70/MASTER.md`及`design-system/70/pages/overview.md`。

2026-07-16，按本节路由实际执行：

- `frontend-design`：明确页面单一任务为“快速判断常用六城最新表现，并从70城中选择城市比较走势”，将0%基准线与月份时间轴作为识别性视觉签名；保留系统字体以符合本项目不远程分发SF Pro的约束。
- `ui-ux-pro-max`：生成并持久化`design-system/70/MASTER.md`与`design-system/70/pages/overview.md`；采用Data-Dense Dashboard建议，补充触控、图表、无障碍和状态反馈检索。工具建议的远程Fira字体、营销CTA和泛蓝橙方案经项目规范否决。
- `impeccable`：运行product register上下文、detector和polish流程；基于现有设计令牌做筛选区层级、内部环境标识和移动端信息优先级打磨，不引入商业化入口。
- 趋势城市选择器接入完整70城：桌面端使用360px搜索浮层，移动端使用底部选择面板；常用六城置顶，其余城市按拼音首字母分组，并支持中文、完整拼音和首字母搜索。已选城市置灰并标注“已选”，达到3城上限后禁用其余选项；允许0城并提供可恢复的空状态，选择结果与URL、图表和图例同步。
- 移动端筛选不再使用顶部图标触发或折叠状态，指标、住宅类型和时间范围作为页面内的全宽内容带持续可见；减少一次操作并让当前数据口径在进入概览前即可确认。

2026-07-18，市场位置增强按同一路由执行：

- `frontend-design`将三项信息组织为一个开放式分析区，以“分布条 + 邻近排名 + 同省完整样本”建立从全国到同级再到省内的阅读顺序。
- `ui-ux-pro-max`采用横向排名、数值直显、移动端卡片化替代表格和44像素触控建议；否决其夸张标题、远程字体、泛蓝配色和营销CTA建议。
- `impeccable`按product register检查单一字体、紧凑层级、语义状态和非卡片化分组；最终对`App.tsx`、`styles.css`和设计令牌运行detector，结果为空。
