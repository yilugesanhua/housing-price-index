# 产品与平台

## Platform

web + native-wechat-miniprogram

## Register

product

## 平台适用标记

本文的现行要求必须使用以下标记，不再依赖“未标注即同时适用”的推断：

- `[共同]`：Web与原生微信小程序共同适用的数据、业务或内容规则。
- `[Web]`：只描述当前响应式Web；不得据此要求小程序改版。
- `[小程序]`：只描述当前原生微信小程序；不得据此要求Web改版。
- `[候选]`：尚未批准的产品方向，不属于当前验收项，也不得与已批准但尚未实现的目标混用。

标记可以直接写在条款、项目、表格行或段落引导句前。带标记的引导句只允许其紧随的同级列表继承相同范围；同一列表混合平台要求时必须逐项标记，不能依靠上下文猜测。

`[共同]` 当前实现与规范出现非安全、非合规的界面差异时，以 `apps/web/` 和 `apps/miniprogram/` 的当前页面为事实基线，先修正文档；任何页面统一或功能新增都必须另行取得用户确认。

## 产品目标

`[共同]` 建设一个基于国家统计局“70个大中城市商品住宅销售价格变动情况”月度数据的住宅价格指数工具，帮助用户快速查看常用六城最新表现，并从完整70城中选择城市比较长期走势。

`[共同]` 当前数据范围：国家统计局完整70城。首页常用六城为北京、上海、广州、深圳、厦门、福州；趋势主图最多同时比较三城。

`[共同]` 产品展示：

- 新建商品住宅、二手住宅。
- 环比、同比。
- 近3年、近5年、近10年。
- `[小程序][partial]` `v2.5.15` 的近15年能力：历史范围为2011年7月至2026年6月，共180个月。100,800条标准记录、当前v7全量审计和单文件候选已通过本地复核；维护人已报告Android和iPhone真机验收通过，但仍须补齐绑定精确候选身份的设备记录，并完成微信开发者工具、平台上传和正式线上复核，因此不是已发布能力。首次打开仍使用近3年、近5年、近10年；可信完整包在后台完成下载和核验后，当前会话才提供近15年。
- 面积筛选使用国家统计局原表的 `全部面积`、`90㎡及以下`、`90-144㎡`、`144㎡以上` 四档；不得改写成“刚需/改善/豪宅”等营销标签。

`[共同]` 产品不展示或估算元/平方米价格，不把指数描述为平均房价，不解释涨跌原因，也不提供投资建议。

## 渠道策略

`[共同]` 当前同时提供响应式Web和原生微信小程序。Web覆盖微信内置浏览器、手机浏览器、桌面浏览器和平板；小程序使用独立原生适配层。两端复用共享数据契约、部分平台无关核心和语义设计令牌；排名、涨跌计数、累计变化等D20所列计算目前仍分别实现，不能概括为已经共享同一计算逻辑。

`[共同]` 渠道优先级：

1. 微信内置浏览器和手机浏览器。
2. 微信小程序。
3. 桌面和平板浏览器。

`[共同]` 不得把桌面页面等比例缩小后作为移动版，也不得用WebView简单包裹网页冒充完成小程序适配。

## 品牌命名

| 平台与使用位置 | 当前显示文案 | 说明 |
| --- | --- | --- |
| Web顶部产品品牌 | `70城住宅指数` | 当前顶部品牌识别 |
| Web顶部次级栏目 | `全国城市观察` | 当前品牌下方栏目，不替代产品品牌 |
| Web页面标题/OG标题 | `70城住宅指数` | 当前静态 `<title>` 与 `og:title`；不随URL筛选动态变化 |
| Web系统分享标题 | `70城住宅价格指数` | 当前浏览器系统分享标题；允许与顶部品牌保留现状差异 |
| Web页面主标题 | `读懂你的城市住宅价格变化` | 面向用户的内容标题，不作为产品名称 |
| Web导航/区块 | `城市概览`、`市场位置`、`长期趋势` | 当前Web信息架构 |
| Web城市术语 | `重点城市` | Web不自动定位 |
| 小程序原生导航标题 | `住房小二` | 与微信公众平台登记名称一致 |
| 小程序原生分享品牌 | `住房小二` | 当前分享标题以该名称开头 |
| 小程序顶部栏目 | `全国城市观察` | 首页顶部栏目名，不替代小程序名称 |
| 小程序页面主标题 | `读懂你的城市住宅价格变化` | 与Web共同的内容标题 |
| 小程序导航/区块 | `城市概览`、`70城温度`、`历史走势` | 当前小程序信息架构 |
| 小程序城市术语 | `定位城市`、`当前城市` | 对应模糊定位与手动选择后的当前查看城市 |

`[共同]` 同一位置只使用矩阵规定的一种名称。矩阵记录的是当前允许保留的平台差异，不要求跨平台统一。调整任一名称前必须核对微信公众平台登记、原生导航、分享文案、页面标题和相关验收项，不得把栏目名或主标题误当作小程序名称。

## 六城概览

`[共同]` 首页用于回答：六座城市最新表现如何，哪些城市上涨或下跌，新房和二手房是否背离。

`[共同]` 首页包含：

- 最新统计月份和官方发布日期。
- 环比/同比、新房/二手房分段控件。
- 六座城市的最新变动率和最近12个月迷你趋势。
- 按最新变动率排序，并明确当前排序指标。
- 数据来源和统计口径入口。

`[共同]` “六城平均”“排名”“连续上涨月数”等本站计算指标必须明确标注，不能表示为国家统计局官方结论。

## 市场位置

`[共同]` 首页在六城概览后提供最新月份的市场位置分析，并与全局环比/同比、新房/二手房筛选同步：

- `[共同]` “70城温度”内部统计上涨、持平、下跌和缺失四类，四类合计必须等于70。正常页面只显示上涨、持平和下跌三项，分布条以非缺失的有效城市数为分母；只有缺失数大于0时才显示异常说明。`null`不得算作持平，也不得参与排名或平均值。
- `[共同]` “同级城市对比”按国家统计局发布的一二三线城市划分，显示当前平台所选城市的同级名次、同级城市简单平均值和平台当前采用的排名列表。
- `[共同]` “省内城市对比”只比较国家统计局70城名单中的同省城市；必须明确其不是该省全部城市。直辖市或样本中仅有一城时显示无法横向比较。
- `[Web]` 继续使用“重点城市”：可从完整70城中独立选择并写入URL；趋势列表不足三座时自动将新重点城市加入趋势比较，列表已满时只切换重点城市。主图清空时仍保留重点城市，Web不自动定位。
- `[小程序]` 把相同的内部 `focusCity` 呈现为“定位城市”或“当前城市”：分享路由优先且不被自动定位覆盖；没有分享路由、也没有手动选择标记时才尝试自动模糊定位。自动定位成功后趋势城市重置为该城市；手动选城时，趋势不足三城才追加，已满时只切换当前城市。
- `[共同]` 城市计数、排名和简单平均均为本站基于官方指数计算，不能表示为国家统计局官方排名、热度评分或投资结论。

## 排名口径

`[共同]` 市场位置中的70城、同级城市和省内城市三个范围分别独立排名，Web与小程序当前均使用竞赛排名：

- 按当前筛选口径的最新变动率从高到低排序；`null`不参与排名。
- 相同数值共享同一名次，后续名次按其实际排序位置跳号。例如 `A=0.3`、`B=0.3`、`C=0.2` 时名次为 `1、1、3`，不是 `1、1、2`。
- 70城、同级和省内均在各自有效城市集合内重新排名，分母是该范围中参与排名的有效城市数。
- 同值城市按 `city_id` 稳定排序，只决定列表展示先后，不打破并列名次。

`[共同]` 常用六城卡片左侧数字只是六城内部排序标识，不是70城、同级或省内名次。当前Web对同值城市仍显示连续位置号，小程序显示竞赛名次；这是当前页面事实差异，本轮不统一、不改页面，后续只有在取得明确确认并完成跨平台验收后才可调整。

## 趋势比较

`[共同]` 筛选器：

- 城市多选，支持从完整70城中搜索，主图可不选城市，最多同时显示三座城市。
- 清空全部城市时显示解释性空状态和“添加城市”入口，不自动恢复默认城市。
- 支持中文城市名、完整拼音和拼音首字母搜索；常用六城置顶，其余城市按拼音首字母分组。
- 已选城市置灰并标记“已选”；达到三城上限后禁用其他城市。
- 环比或同比。
- 新房或二手房。
- 36、60或120个月。
- 面积段筛选与新房/二手房、环比/同比、趋势和市场位置联动；缺失值保持缺失，不按0计入变动率。

主图与辅助数据按平台适用：

- `[共同]` 百分比纵轴和清晰的0%基准线；缺失数据断线，不绘制为0。
- `[共同]` 不在同一张图叠加环比、同比、新房、二手房和六座城市全部曲线。
- `[共同]` 主趋势图下方提供累计变化图：所选范围首月设为100，从第二个月起按同一住宅类型的月度环比指数逐月复合；该图不受环比/同比主图切换影响。
- `[共同]` 累计变化只表示相对路径，不标为元/平方米房价；缺少环比数据时从缺口起停止后续计算，不跨越缺口拼接。
- `[Web]` 支持缩放、拖动和图例显隐，120个月视图自动减少横轴标签；Tooltip显示统计月份、原始指数、变动率和发布日期，统计口径变化可使用时间轴标记。
- `[Web]` 累计图Tooltip同时显示相对起点变化和“较高点”变化；“较高点”只取当前月份及以前已出现的最高累计值，不使用未来月份数据。
- `[Web]` 当前提供折叠的精确数据入口，桌面使用表格，手机使用月份选择与按城市分组列表。
- `[小程序]` 当前使用wx-f2图表、触摸滑动查看月份、图例显隐和累计变化图；当前页面不展示精确数据入口，也不以Web缩放按钮、键盘焦点圈定或Web表格作为小程序验收要求。

## 微信访问与分享

- `[共同]` 首屏优先显示最新统计月份、当前筛选和六城概览，不要求登录。
- `[Web]` 手机端统计月份在顶部只显示一次，官方发布日期紧随页面说明；筛选按当前两行结构持续可见，优先让六城概览更早进入视口。
- `[共同]` 趋势城市、Web重点城市或小程序当前城市、指标、房屋类型和时间范围编码为可恢复的URL或平台路由参数。
- `[Web]` 首屏只加载六城最近12个月摘要、最新月份70城四档面积市场快照、温度历史和当前选择城市的趋势分片，不下载完整70城全量文件；新增城市时按需加载并长期缓存对应分片。
- `[小程序]` 发现新月份或通过受审计协议的同月历史修订后，一次性下载包含完整70城历史的紧凑数据包，完成版本、范围、70城、记录数、修订链和SHA-256校验后再原子切换并保存在本地；切换城市不得再次下载该城市数据。下载或校验失败时只使用最近一份未被撤销且验证通过的缓存或内置数据；不存在时明确显示数据暂不可用。
- `[Web]` 图表模块在摘要数据就绪后异步加载，六城概览和筛选不等待ECharts解析。
- `[Web]` 查询参数使用版本字段 `v=1`。无效城市、指标、房屋类型或时间范围回退默认值；超过三座城市时保留前三座并显示提示。
- `[Web]` 参数按固定顺序 `v / metric / type / range / cities / focus / size` 和规范值序列化；`focus`可独立于`cities`，缺失时回退第一座趋势城市，无趋势城市时回退默认重点城市。忽略未知参数；参数不得拼接为文件路径、脚本、HTML或任意网络地址。
- `[Web]` 旧版参数必须通过显式迁移规则处理；无法迁移时回退默认状态并提示链接版本过旧。
- `[Web]` Web当前静态实现使用固定的分享标题和缩略图；分享链接打开后恢复筛选状态。
- `[Web]` Web页面提供稳定的 `title`、`description`、`og:title`、`og:description` 和 `og:image`；这些固定元数据不能声称反映URL中的动态筛选结果。
- `[Web]` 与当前筛选匹配的动态分享标题和缩略图不属于纯静态Web能力。只有增加微信JSSDK签名服务后，Web端才实现动态分享卡片。
- `[小程序]` 小程序使用平台原生分享生命周期生成动态分享内容。
- `[共同]` 分享文案描述数据范围，不使用“暴涨”“抄底”“最值得买”等投资导向措辞。
- `[Web]` 微信内置浏览器的核心流程不依赖下载、右键、鼠标悬停或新窗口。
- `[共同]` 来源、统计口径和数据状态集中在来源区；页脚不重复完整免责声明和数据来源。
- `[共同]` 用户拒绝授权时仍可查看公开数据；收藏、提醒和同步等确需账户的功能再按需申请最小权限。

## 不可用数据状态

`[小程序]` 随代码包发布、已经通过构建时全量数据审计且在客户端完成结构、数值和派生结果校验的内置快照，是不依赖远程控制面的独立可信基线。首次安装、清除本地状态、离线、远程控制暂不可得或已有控制状态超过有效期，都不得单独使该内置快照失效；控制状态过期只禁止它继续授权新的远程切换，并触发刷新和数据状态陈旧提示。客户端已经持久化的撤销事实，或在线取得并完整验证的更高代次控制状态中的撤销事实，必须单调保留并优先于内置/缓存优先级；已明确撤销的数据包或源版本不得因离线、重启或回退而恢复。远程包下载、哈希、结构或完整重建失败时，继续使用当前已验证且没有已知撤销的缓存或内置快照。只有全部本地候选自身校验失败，或都已被当前已知撤销事实排除且没有完整验证的安全替代时，才进入独立的 `unavailable` 状态。合法受控回滚是普通月份防降级和“不得早于内置月份”比较的唯一例外：即使目标月份早于内置快照，只要更高控制代次、当前不可变撤销登记、被撤销活动版本、精确替代目标以及目标数据包/源版本完整验证形成闭环，就必须允许安全目标参与恢复；不能满足时只能使用另一份已验证且未撤销的本地数据，否则进入 `unavailable`，不得恢复被撤销坏包。该状态不是全 `null` 的正常快照，不生成或展示70城排名、同级/省内平均、温度、趋势、累计变化和任何市场方向结论；只提供数据不可用说明、重新检查和来源/审计入口。有效数据下的当前Web和小程序页面、文案、布局及交互不因本规则改变。

## 当前平台实现

- `[Web]` 使用Vite、React、TypeScript和ECharts，按清单读取摘要、市场快照、温度历史和所选城市分片。
- `[小程序]` 已采用原生实现和wx-f2，源码位于 `apps/miniprogram/`；微信开发者工具目录只是同步副本。
- `[共同]` 两个平台客户端都不得抓取国家统计局页面，也不得绕过发布清单和数据校验。
- `[小程序]` 唯一机器可读源码版本来自 `apps/miniprogram/config/version.js`，截至2026-08-06的当前值为 `v2.5.15`。本文只记录该次文档整理时的快照，不是第二版本源，也不自动证明已完成开发者工具、双真机、微信审核或正式发布。

## 跨平台计算一致性

`[共同]` Web和小程序目前存在分别实现的排名、涨平跌计数和累计变化计算。后续重构前必须先建立版本化跨平台结果契约：固定输入fixture和独立期望输出都记录SHA-256，期望输出由当前Web、小程序与数据契约人工复核后冻结，不能由任一被测实现临时生成。矩阵至少覆盖新房/二手房、四档面积、环比/同比、同级/省内/70城、36/60/120个月，以及并列、缺失、允许的 `null`、连续月份和缺口边界；排名顺序、并列规则、涨平跌缺失计数、平均、温度、趋势和累计变化全部逐值核对。只有两端分别与同一独立期望输出一致后，才允许把重复逻辑逐步迁入 `packages/core/`；重构前后当前页面可见结果和交互必须保持不变。

## 实现演进与当前范围

`[共同]` 以下状态不得混用：`current` 表示当前源码中完整存在、可按对应验收入口核对的能力；`partial` 表示已有实现基础，但正确性门禁、测试、部署、平台验收或其他关闭条件仍未齐备，不能作为正式生产能力；`approved_not_implemented` 表示目标已经批准，但尚无足以进入 `partial` 的实现证据；`candidate` 表示尚未批准的方向且不是实施待办；`out_of_scope` 表示当前明确不做。规范批准、源码存在、本地测试、CI、云端部署、微信审核、正式发布和稳定归档仍是互不替代的状态。

| 状态 | 平台 | 能力或方向 | 验收/治理入口 |
| --- | --- | --- | --- |
| `current` | Web | 完整70城、常用六城概览、市场位置、最多三城趋势、四档面积和响应式访问 | 当前 `apps/web/` 与适用Web验收项 |
| `current` | 小程序源码 | 当前同口径页面、定位城市选择和原生分享 | 当前 `apps/miniprogram/`；源码存在不代表开发版、体验版、审核版或正式版状态 |
| `current` | 共同 | 面积四档筛选、本地筛选状态与清除入口、公开数据无需登录 | 当前源码和适用平台验收项 |
| `partial` | 小程序远程数据与自动更新 | 完整70城远程数据包下载、校验、缓存和切换已有客户端及自动化基础；安全恢复、受保护发布、完整验证、云端部署和微信平台验收仍以实施状态登记为准 | `docs/IMPLEMENTATION_STATUS.md`；不得描述为已启用、已部署、已通过或已正式发布 |
| `partial` | 小程序 `v2.5.15` 近15年完整包 | 2011年7月至2026年6月的180个月完整快照；首屏后自动后台检查并下载单个完整远程数据文件，只有全量验证通过后才原子替换当前完整快照。不得按城市、月份或指标拆给用户，不得把内置10年数据与单独下载的5年尾段混用；下载完成后当前会话直接可用，不强制重开、不自动切换用户已选时间范围、不重置城市、筛选或滚动位置 | 100,800条官方记录、v7审计、`v2.5.15`不可变候选和本地自动化已通过；维护人报告双真机基础验收通过，但尚未补齐绑定精确候选的设备字段。正式云端、开发者工具构建、平台上传、审核和线上复核完成前不能描述为已发布能力 |
| `partial` | 自动更新安全路径 | `docs/IMPLEMENTATION_STATUS.md` 中已有局部或本地实现、但验证、提交、部署、平台验收或其他关闭证据尚未齐备的安全、权限和恢复项目；本地 `implemented/passed_limited` 不代表远端 `main` 已包含、CI已通过或生产能力已部署 | `docs/IMPLEMENTATION_STATUS.md`中的逐项实现、验证和证据状态；不在本文重复维护编号状态 |
| `approved_not_implemented` | 自动更新门禁与归档 | `docs/IMPLEMENTATION_STATUS.md` 中 `spec_status=approved` 且 `implementation_status=not_started` 的目标，例如I10尚未实现的原字节晋级、最终`release-manifest`生成器和跨环境复现，以及I14版本漂移门禁 | `docs/IMPLEMENTATION_STATUS.md`；不得因规范已经定义而视为实现，具体编号状态只在该登记中维护 |
| `candidate` | 共同 | 六城平均、连续涨跌月数、CSV下载、单城市详情和面向用户的修订记录页 | 尚未批准，不进入当前验收或实施待办 |
| `candidate` | 共同 | 关注城市、保存比较、提醒、报告、嵌入图表、数据接口、候补名单和机构咨询 | 仅用于需求验证，不承诺账户或跨设备同步 |
| `candidate` | 后续平台 | 账户同步、微信订阅消息、支付、广告、机构服务后台、第三方数据API和多渠道提醒 | 必须先完成需求、合规和单独产品批准 |
| `out_of_scope` | 共同 | 元/平方米价格、成交量、库存、土地和政策分析；地图作为主要交互；主图超过三城；强制登录或诱导分享 | 不得作为当前功能实现 |
| `out_of_scope` | 共同 | 用虚构数据冒充官方数据，或隐藏统计口径、缺失数据和来源信息 | 永久禁止 |
