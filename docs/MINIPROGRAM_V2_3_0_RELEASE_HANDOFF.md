# v2.3.0 上线交接单

更新日期：2026-07-29  
候选代码提交：`57941e2d5318a1680fe9828720ec06fe85474d8e`  
测试与记录提交：`5618b96c94180dd1f6d58a2b572e90b3ba4aec7a`

## 已自动完成

- `npm run check` 和 Web 端到端测试在 GitHub CI `30412654235` 通过。
- 小程序源目录已重新同步到 `70城小程序技术验证/`，两边应用文件逐文件 SHA-256 一致。
- 小程序自动化测试 99/99 通过；包含70城120个月、失败关闭、同月旧源拒绝、离线回退、一次下载完整70城、切城零下载、定位和图表规则。
- 最终候选12个月云端全链路回放 `30411300588` 的12轮全部通过，生产只读复核 `30412319177` 通过。
- 微信开发者工具稳定版 `2.01.2510290`、基础库 `3.17.0` 编译预览成功；预览包 `1,594,604` 字节，低于2MB。
- `2.3.0` 已通过微信开发者工具CLI重新上传到 AppID `wxf2b2a44f4c788def` 的开发版本区，上传包 `1,603,517` 字节。最终候选已恢复用户确认的定位入口结构；这不是正式发布，也没有提交审核。
- 仓库和生产Environment的 `AUTOMATIC_RELEASE_ENABLED` 均为 `false`；写入和只读监测凭据名称存在，仓库高置信扫描未发现腾讯云密钥或私钥正文。

## 项目所有者现在操作

1. 登录微信公众平台，进入“小程序 -> 管理 -> 版本管理”，确认开发版本中存在 `2.3.0`，将它选为体验版。
2. 在“成员管理 -> 体验成员”中加入用于验收的微信号，用 Android 和 iPhone 分别扫描体验码。
3. 两台设备严格完成 [`MINIPROGRAM_V2_3_0_DEVICE_TEST.md`](MINIPROGRAM_V2_3_0_DEVICE_TEST.md) 的12项测试并填写机型、系统、微信版本、时间和证据；任一项失败先停止审核并反馈。
4. 确认一个正式纠错入口：提供可公开的纠错邮箱，或确认微信公众平台中已启用且实际可用的客服入口。没有入口不得提交正式验收。
5. 把双真机结果和纠错入口发回本项目。确认24项全部通过后，再创建只读 `v2.3.0_2026-07-29` 稳定归档，随后在微信公众平台提交审核。

## 审核发布后

- 先不要立即开启自动生产发布。用正式发布版打开小程序，确认内置修正版正常显示、同月旧远端包被拒绝、来源页和定位功能正常。
- 完成一次正式客户端远程读取复核后，由项目所有者单独明确授权，才把生产 `AUTOMATIC_RELEASE_ENABLED` 改为 `true`。
- 开启后第一次月度自动发布仍执行V7解析、V4全记录审计、560条/70城/8口径、历史零修改、完整包哈希、云端回读和发布后监测；任一项失败保留上月数据。

## 当前不得做

- 不得在双真机24项完成前创建稳定归档或提交微信审核。
- 不得用开发者工具预览代替Android/iPhone真机证据。
- 不得为了赶时间跳过数据门禁、覆盖已有远端版本或提前开启自动生产发布。
