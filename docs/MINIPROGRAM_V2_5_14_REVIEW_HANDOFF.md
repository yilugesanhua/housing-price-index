# v2.5.14 微信审核交接记录

> 本记录只对应当前小程序源码候选 `v2.5.14`。它不是微信审核通过、正式发布或线上回读证明；平台状态必须以微信开发者工具和微信公众平台现场结果为准。

## 上传身份

| 项目 | 当前值 |
| --- | --- |
| 小程序版本 | `v2.5.14` |
| 上一线上版本 | `v2.4.9` |
| 唯一机器版本源 | `apps/miniprogram/config/version.js` |
| 开发者工具目录 | `70城小程序技术验证/` |
| 数据截止月份 | `2026-06` |
| 数据覆盖 | 内置快照最近120个月；云端完整包180个月（近15年） |
| 云端数据更新 | 首屏使用内置快照，后台自动检查并校验云端完整包；成功后当前会话可使用15年范围 |

## 已完成的工程证据

- `apps/miniprogram/config/version.js` 与开发者工具目录版本均为 `v2.5.14`。
- 已运行 `npm.cmd run miniprogram:sync`，同步脚本正常结束。
- 小程序自动化测试 `npm.cmd run test:miniprogram`：335/335 通过。
- 版本更新提示、完整15年包下载、哈希校验、原子缓存切换、坏包拒绝、断网回退和存储不足保护均有自动化覆盖。
- Android 与 iPhone 真机均已由维护人现场确认：显示“云端数据已更新”，近15年选项可用，数据范围为180个月。

## 仍需平台现场完成

- 在微信开发者工具中确认当前打开的是 `70城小程序技术验证/`，重新编译后再上传，不要使用旧项目缓存。
- 上传后确认微信平台回读版本号为 `2.5.14`。
- 审核前补齐隐私保护指引、模糊位置信息用途、数据来源说明和测试账号/路径（如平台页面要求）。
- 提交审核后等待平台结果；审核通过前不得描述为已发布。
- 正式发布后再现场回读线上版本号、首屏数据状态、近15年范围和云端更新时间。

## 微信上传页建议

- 更新类型：选择“版本升级”。本次包含近15年数据和版本更新处理，不应选择“修订补丁”。
- 版本号：填写 `2.5.14`，不要填写 `2.4.10`。
- 项目备注：

  `v2.5.14：新增近15年（180个月）官方70城住宅价格指数数据，支持云端数据自动更新；优化完整数据包缓存校验和异常回退，修复部分设备仍显示内置数据的问题。已完成Android和iPhone真机验证。`

## 状态边界

| 阶段 | 状态 |
| --- | --- |
| 源码候选 | 已完成本地检查 |
| 开发者工具同步 | 已完成 |
| Android/iPhone真机 | 已确认基础使用和云端数据更新 |
| 微信开发版本上传 | 待维护人操作并现场回读 |
| 微信审核 | 待提交 |
| 正式发布 | 待审核通过后操作 |

