# v2.4.1 小程序上线审查单

> **状态：未执行的当前版本模板**
> 本文件只定义 `v2.4.1` 独立审查需要收集的证据。所有结果填写并经复核前，不构成CI通过、真机通过、微信审核、云端部署或正式发布证据。

提交前工作区执行的本地实现与 `passed_limited` 证据只在 [`IMPLEMENTATION_STATUS.md`](IMPLEMENTATION_STATUS.md) 登记；当前实现已固定在精确提交 `cdea2207ff8f570aa1d8725ea474f22df30f26c8`，普通CI `30736720927` 仅是标准检查证据。上述内容不勾选本表任何项目，也不得用于填写生产迁移、云函数部署、开发者工具、双真机或微信平台结果。

## 候选身份

| 字段 | 当前值 |
| --- | --- |
| 小程序版本 | `v2.4.1` |
| 精确Git SHA | 待填写 |
| 不可变候选ZIP/SHA-256 | 待由确定性候选工具生成 |
| `candidate-manifest.json` SHA-256 | 待生成；所有后续证据绑定该身份 |
| 内置数据截止月份 | `2026-06`（发布前从候选快照重新读取） |
| 内置源数据版本 | `2026-06-4fd1d1a8ff12`（发布前重新核对） |
| 解析器 | `official-html-v7-product-housing-only`（发布前从审计读取） |
| 独立审计器 | `full-record-audit-v5`（发布前从审计读取） |
| 微信开发者工具/基础库 | 待填写 |
| 审查人/时间 | 待填写 |

任一身份在审查后变化，全部结果失效并从头执行。短SHA、工作区当前状态、`v2.4.0` 本地测试或 `v2.3.0` 平台记录不能替代精确候选身份。

## 自动化与数据证据

- [ ] `npm run check` 在候选精确提交的CI运行中通过，并记录运行ID、URL和日志构件SHA-256。
- [ ] `npm run test:e2e` 在同一候选提交通过；记录桌面和手机视口证据。
- [ ] 小程序源目录与 `70城小程序技术验证/` 逐文件一致。
- [ ] 微信开发者工具重新编译、预览和上传 `2.4.1`；记录工具版本、基础库、包体积和证据SHA-256。
- [ ] 普通月度更新12轮在 `v2.4.1` 精确提交全部通过，报告身份和逐阶段耗时完整。
- [ ] 历史修订专项12轮与故障矩阵在同一提交全部通过；三态重跑、双重撤销、一般legacy隔离、唯一迁移正反向、控制/回执过期、不安全回退和崩溃恢复均按规范失败关闭或仅执行获准动作。
- [ ] 客户端精确70城集合、全部series/latest/breadth、派生值、逐文件落盘回读、单一状态、孤儿清理和 `unavailable` 页面短路均绑定当前候选测试结果。
- [ ] 候选内置数据、回放输入、解析器、审计器及远程协议身份完全一致。
- [ ] 仓库和生产Environment的自动发布开关状态经过现场复核并记录；不得沿用旧截图或旧运行摘要。
- [ ] 候选ZIP和 `candidate-manifest.json` 已确定性生成并回读，CI、开发者工具和后续双真机证据均绑定同一候选；此时不要求稳定归档已经创建。

## 生产控制迁移证据

| 字段 | 当前值 |
| --- | --- |
| 迁移状态 | `not_run` |
| 迁移验证状态 | `not_verified` |
| 迁移ID | `legacy-control-2026-06-e9788d0bddf3` |
| 迁移精确Git SHA/同提交CI运行ID | 待填写 |
| 受保护迁移运行ID/Environment批准证据 | 待填写 |
| 迁移前 `current.json` 预期SHA-256 | `8fae20cb98e56d3321be45306a8e2fdbef9e2dc9482791fab79c0687b3de2f4e`；仍须现场原字节回读 |
| 迁移前现场SHA-256/迁移后SHA-256 | 待填写 / 待填写 |
| 迁移后撤销登记SHA-256/代次 | 待填写 / 待填写 |
| `validator_contract_sha256` | 待填写；Intent v3固定契约哈希，不表示旧云函数已部署 |
| `describe_validator_observed` | `false`；待新版严格函数部署后收尾阶段填写 |
| `describe_validator_verified` | `false`；待收尾阶段核对固定三字段及环境/函数身份 |
| `cloud_function_response_observed` | `false`；待受保护运行现场填写 |
| `strict_validator_verified` | `false`；待独立部署及新鲜有效回执证明 |
| 一次性授权运行后状态 | 待回读；必须为 `false` 或未设置 |

- [ ] `ready/already_migrated/conflict` 判定、旧manifest/bootstrap原字节、迁移前后指针和撤销登记均有可回读证据；冲突时指针保持观察到的原始字节。
- [ ] 迁移前后两次完整70城逐值验证、数据包/源版本双重撤销、代次和下一次普通发布保留撤销均通过。
- [ ] 写入阶段已按Intent v3完成旧指针原字节复核、双重撤销登记、指针切换和直接完整70城回读；该阶段不调用旧云函数的`describe_validator`。
- [ ] 新版严格函数部署后，收尾阶段的`describe_validator` 只读预检字段集合和三个固定值、精确环境/函数、运行ID及响应哈希全部通过；预检失败时保持已观察的精确指针状态并转入受控收尾恢复。
- [ ] 写后严格云函数回执的schema、验证器身份、时间、当前指纹、manifest/撤销哈希和两类代次全部通过；预检通过或只观察到响应均不得替代本项。
- [ ] `LEGACY_CONTROL_MIGRATION_AUTHORIZED` 已撤销，`AUTOMATIC_RELEASE_ENABLED` 与 `PRODUCTION_RELEASE_AUTHORIZED` 始终保持关闭或未设置。

本节全部待填项没有受保护生产证据前保持未通过。迁移通过也只解除唯一旧指针阻断，不构成自动发布、云端能力、双真机或正式版发布通过。

## 范围与外部证据

- [ ] Android和iPhone均完成 [`MINIPROGRAM_V2_4_1_DEVICE_TEST.md`](MINIPROGRAM_V2_4_1_DEVICE_TEST.md)，所有必测项有原始证据。
- [ ] 正式纠错入口、主体用途及数据转载/商业使用边界完成当前版本复核。
- [ ] 微信隐私保护指引、`app.json`权限/接口、来源页用户可见文案和云函数/腾讯位置服务路径逐项一致；平台侧内容必须现场读取，不能由源码推断。
- [ ] 微信公众平台中的开发版、体验版、审核版和正式版版本号逐步记录，不把源码版本号当作平台状态。
- [ ] [`MINIPROGRAM_V2_4_1_RELEASE_HANDOFF.md`](MINIPROGRAM_V2_4_1_RELEASE_HANDOFF.md) 的全部前置证据绑定同一候选身份。

## 结论

当前结论：`not_tested`。

只有上述项目全部通过、证据可回读且身份一致后，才能填写最终审查结论。通过后才允许把已验收候选ZIP原字节晋级为稳定归档并生成最终 `release-manifest.json`；不得为归档重新压缩候选。旧版审查文件只读保留，不得复制其通过项目到本文件。
