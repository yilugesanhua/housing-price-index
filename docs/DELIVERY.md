# 第一阶段交付记录

## 2026-07-20 微信访问与小程序适配加固

- Web 数据加载拆成摘要/市场/温度首屏和趋势城市分片两阶段；单个城市分片超时或失败时，六城概览和市场位置仍可用，并提供“重试趋势”。
- 增加离线状态、网络恢复自动重试、微信返回缓存恢复和回到前台超过5分钟后的清单刷新；微信 UA 下分享按钮改为剪贴板回退并提示右上角菜单分享。
- 低高度横屏取消顶部粘性占屏的分区快捷链接，优先保留四项直接筛选；城市底部面板使用 `visualViewport` 高度和旧版 `vh` 回退，键盘弹出时不遮挡完成按钮。
- 共享核心新增 `HousingViewState`、`ViewRange` 和默认视图；设计令牌从 `tokens.css` 生成 `tokens.wxss`、`tokens.json`，`npm run design-tokens:check` 检查跨平台漂移。
- 新增针对性E2E：微信分享回退、单城市趋势分片失败、离线恢复和低高度横屏；桌面Chromium与手机WebKit均通过。

## 2026-07-19 摘要、分享与极窄屏复查

- 修复重点城市摘要负值显示为“下降 +5.5%”的问题，统一使用“下降 5.5%”文案，并新增无符号百分比格式化测试。
- 分享按钮改为系统分享、剪贴板和页面内兼容复制三级回退；取消原生 `prompt`，明确标注“分享当前页面”。
- 按响应式断点只渲染一套筛选入口：桌面为完整筛选面板，手机及低高度横屏为粘性快速筛选，避免隐藏DOM造成重复无障碍入口。
- 320–340px快速筛选改为两列两行并保持16px字号，重点摘要改为窄屏上下布局；响应式E2E覆盖320px和844×390横屏。
- 本轮复查使用2026-07-19获取的 Vercel Web Interface Guidelines；Impeccable detector无命中，lint、typecheck、Web单测17/17通过，针对性E2E 6/6通过。

## 2026-07-19 月度数据发现与告警

- 新增 `npm run data:check-latest`，复用官方发布列表发现脚本，比较官方最新统计月份、当前发布月份和 `next_check_due_at`。
- 新增 `scripts/data/check-latest.ts` 及4项单元测试；输出 `report.json` 和 `report.md`，状态分为正常、发现新月份和异常。
- 新增 `.github/workflows/monthly-data-check.yml`，每月13日至22日北京时间10:30运行；报告写入Job Summary并上传30天构件，发现新月份或异常时任务失败以触发仓库通知。
- 该流程明确不自动发布数据，仍须执行现有抓取、逐条核验、数据校验、构建和发布门禁。

## 2026-07-19 审查项落地

- 修复320px传统滚动条环境下的页面最小宽度溢出；移动粘性导航和分区锚点偏移纳入顶部安全区。
- 移动端图表包裹层裁剪ECharts内部在极窄视口产生的横向绘图区，避免累计图内部宽度把页面撑出滚动条。
- 桌面端恢复只读口径标签，快速可编辑筛选仅保留在移动粘性导航；常见手机宽度选择器字号提升至16px，340px以下窄屏使用紧凑回退。
- 844×390等手机横屏视口按低高度条件继续使用移动紧凑筛选和内容重排，不再误入桌面完整筛选布局。

## 2026-07-19 快速筛选颜色语义收敛

- 按 `impeccable quieter` 将粘性导航中的住宅类型、指标、面积段和时间范围统一为中性控件样式，不再使用新房橙色或指标蓝色。
- 蓝色仅表示当前页面导航选中态，红绿仅表示上涨和下跌，避免筛选值与城市概览选中状态产生视觉误解。

## 2026-07-19 移动端筛选入口合并

- 手机端隐藏顶部重复的大型筛选面板，粘性导航扩展为住宅类型、指标、面积段和时间范围四个等宽原生选择器，并在导航第二行加入恢复默认图标。
- 桌面端继续保留完整筛选面板；移动端所有筛选能力、URL同步、撤销和重置能力保持不变，同时缩短首屏高度。

## 2026-07-19 粘性导航快速筛选

- 将粘性数据导航中的住宅类型、指标和面积段只读标签升级为原生紧凑选择器，滚动到城市概览、市场位置或长期趋势时可直接修改核心筛选，无需返回页面顶部。
- 顶部完整筛选区继续提供相同口径、时间范围和重置操作；快速筛选与URL、数据请求及全部模块共用同一状态。移动端快速筛选和导航锚点保持44px触控高度且不横向溢出。

## 2026-07-19 重点城市入口与趋势标题去重

- 重点城市只保留页面顶部原生选择器作为唯一修改入口；市场位置标题区改为只读展示当前重点城市，不再重复显示趋势城市快捷切换按钮。
- 手机端长期趋势的“恢复默认城市”改为标题右侧44px刷新图标按钮，保留完整无障碍名称和提示，避免按钮换行占用独立一行。

## 2026-07-19 移动端数据导航去除横向滚动

- 手机端页面数据导航改为两行布局：第一行显示住宅类型、指标和面积段口径，第二行三等分显示城市概览、市场位置和长期趋势。
- 移除导航横向滚动，并将移动端三个分区的粘性导航滚动偏移统一调整为172px；响应式E2E新增导航自身无横向溢出断言。

## 2026-07-19 重点城市控件宽度优化

- 手机端重点城市行改为“标签 + 自适应下拉框”布局，移除210px宽度上限，让下拉框填满剩余可用空间，减少右侧空白和孤立边框感。
- 保持原生选择器、44px触控高度、重点城市URL状态和桌面端紧凑布局不变。

## 2026-07-19 移动端筛选区留白调整

- 按 `impeccable layout` 检查结果，将手机端筛选组内标签与控件的间距由4px调整为8px，筛选行之间由12px调整为16px。
- 保持指标、住宅类型、时间范围和面积段两列等宽，以及44px触控高度；不改变筛选状态、URL同步或重置操作位置。

## 2026-07-19 温度历史工具区分层

- 桌面端将温度历史图下方的月份明细与滑块压缩为无边框轻量工具区，降低其与图表Tooltip重复展示时的视觉权重，同时保留精确选月能力。
- 手机端继续使用有边界的完整月份明细区，并为月份滑块保留至少44px触控高度；键盘操作和图表外等价数据不受影响。

## 2026-07-19 分区导航锚点修复

- 为“城市概览 / 市场位置 / 长期趋势”统一增加粘性顶栏滚动偏移，避免点击后分区标题被顶部导航遮挡；320px导航换行状态使用独立偏移。
- E2E逐一点击三个锚点并测量分区顶端与粘性导航底部距离，要求至少8px。

## 2026-07-19 温度图有效城市统计

- 70城温度和温度历史只展示上涨、持平、下跌三类有效结果；移除Tooltip、堆叠段、月份摘要和屏幕阅读文本中的“缺失”项目。
- 缺失值仍保留在原始数据与审计字段中，不被改写为0或涨跌；若历史月份存在缺失，分布条按实际有效城市数计算，并显示“有效城市数 / 70”。

## 2026-07-19 重点城市选择器对齐修复

- 修复重点城市选择器外层容器与原生下拉框宽度不一致的问题：选择器本体现在填满容器，箭头与边界对齐，桌面和移动端均保持无空白缝隙、无水平溢出。
- 通过 Impeccable 布局检测、Web lint/typecheck，以及重点城市和响应式 E2E（4/4）；补存桌面控件截图至 `docs/screenshots/focus-city-control-desktop.png`。

## 2026-07-19 消费者阅读路径优化

- 按 `frontend-design`、`ui-ux-pro-max` 和 `impeccable polish` 优化首屏到图表的阅读路径：新增完整70城重点城市入口、事实摘要和“城市概览/市场位置/长期趋势”粘性导航。
- 当前新房/二手房、环比/同比和面积段口径集中到一组全局标签，移除三个模块标题旁的重复标签；重点城市与趋势城市改为独立且可理解的两个状态，趋势列表有空位时自动同步重点城市。
- 单城市省级区域不再占满第三列；70城温度历史新增月份滑块、点击选月、当前月明细和最新月份突出；趋势图明确城市线色不代表涨跌。
- 累计图移除视口相交后才初始化的条件，改为确定性渲染并保留骨架，桌面和手机E2E均检查Canvas实际绘制像素。
- 修正320px超窄屏下粘性导航与重点城市摘要的48px水平溢出：导航在超窄屏改为三等分第二行，摘要保持方向点、城市和事实文本三列语义结构。
- 最终手机与桌面截图更新于`docs/screenshots/`，响应式矩阵覆盖320、375、384、390、768、1366、1440和手机横屏；`npm run check`、32项桌面Chromium/移动WebKit E2E及Impeccable检测全部通过。

## 2026-07-19 面积分档与70城温度历史

- 按 `frontend-design` 与 `ui-ux-pro-max` 校准为面向普通家庭的移动优先官方数据工具；沿用克制的iOS工具风格，未采用skill建议中的营销式大标题、远程字体或单一绿色主题。
- 面积筛选开放国家统计局原表四档：全部面积、90㎡及以下、90-144㎡、144㎡以上，并联动六城概览、市场位置、趋势、累计图和URL状态。
- 新增“70城温度走势”堆叠图，按当前新房/二手房、环比/同比、面积和时间范围展示上涨、持平、下跌与缺失城市数；提供触摸提示、ARIA描述、最新月份文字摘要和减少动画支持。
- 当前发布清单为schema 1.3.0：126个官方批次、70,560条标准记录、576条六城摘要、560条市场快照、2,016条温度历史及每城1,008条分片记录。`data/audit-report.json`为`full-record-audit-v3`并全部通过。
- E2E新增面积切换、口径标签同步、官方值变化和温度历史Canvas绘制检查；最终截图为`docs/screenshots/mobile-390x844.png`与`docs/screenshots/desktop-1440x900.png`。

## 2026-07-18 趋势上下文与空状态

- 按 Impeccable 产品工具规范将市场方向统一为上涨红色、下跌绿色，并拆分成功、错误与市场涨跌令牌，避免数据语义影响系统状态。
- 主趋势图允许0座城市；清空后保留最后观察城市，URL以 cities= 稳定保存空选择，空状态可直接添加城市或恢复默认城市。
- “趋势比较”标题旁新增住宅类型与指标标签，随新房/二手房、环比/同比筛选即时同步；URL单测与桌面/手机E2E覆盖清空、刷新语义和重新添加流程。
- 常用六城概览、市场位置与趋势比较改为复用同一个口径标签组件；排序说明和“最新月份 · 本站计算”独立作为辅助信息，消除同一口径的多套视觉表达。

## 2026-07-18 final verification refresh

- `npm run check`: passed. Web unit tests 15/15, data tests 12/12, release gate tests 3/3, 17,640 published records validated, production build passed.
- `npm run test:e2e`: passed 26/26 across desktop Chromium and mobile WebKit. Final screenshots are in `docs/screenshots/`.
- Production assets: entry JavaScript 192.10 kB raw / 61.35 kB gzip; lazy trend chart 563.09 kB raw / 189.02 kB gzip; CSS 31.48 kB raw / 6.25 kB gzip. The chart chunk warning is retained as a known optimization follow-up.
- Browser verification at 393x852 confirmed no horizontal overflow, city-sheet focus/scroll-lock/restore behavior, mobile exact-data list, and the 70-city selector. The app-source Impeccable detector returned an empty result.

## 2026-07-18 市场位置增强

- 按 `frontend-design`、`ui-ux-pro-max` 和 `impeccable` 的产品工具规范新增“70城温度、同级城市对比、省内城市对比”；拒绝工具生成的营销式蓝色房产看板、超大标题和热度评分建议，沿用现有克制的iOS风格。
- 发布清单升级为schema 1.2.0，新增最新月份140条市场快照：70城×新房/二手房，全部为`size_band=all`；浏览器继续不读取17.5MB全量数据。
- 共享核心新增国家统计局一二三线城市分类、70城所属省级区域、涨平跌计数、并列排名、同级简单平均和省内排序。省内对比明确只覆盖70城样本，不表示全省所有城市。
- 新房、二手房和环比、同比均复用页面全局筛选；观察城市在当前趋势城市中切换。新增核心单测与桌面/手机E2E验证。
- 最终`npm run check`和24项E2E全部通过；375/390/768/1366/1440及横屏视口无水平溢出，最终手机和桌面截图已人工检查，Impeccable应用源检测返回空结果。

以下“当前数据”和“已执行验证”记录的是2026-07-16历史交付基线，最新状态以上方2026-07-19章节为准。

更新日期：2026-07-16

## 当前数据

- 覆盖范围：2016-01至2026-06。
- 生产批次：126个官方HTML批次。
- 标准记录：17640条，覆盖70城、新房/二手房和环比/同比。
- 数据版本：`2026-06-611d87e6ba03`；解析器版本：`official-html-v5-exclude-size-band-tables`。
- 来源审计：`data/audit-report.json`，126批次和17640条记录全部通过SHA-256、官方URL、标题月份、发布日期、独立表类型、总体/面积分类、记录定位和原始单元格回查。
- 修订历史：`data/normalized/revisions.json`保留19152条追加修订，包括此前1512条解析器元数据迁移记录，以及本次17640条全量重新解析修订；历史未删除。

## 已执行验证

- `npm run check`：最终复跑通过；Web单测10项、数据测试12项、发布门禁单测3项、发布数据17640条校验通过，生产构建成功。JS gzip 245.20 KB，CSS gzip 5.07 KB；存在单个JS chunk压缩前超过500 kB的Vite警告，但JS与CSS gzip合计仍低于500 KB项目目标。
- `npm run test:e2e`：最终复跑22项全部通过（桌面Chromium与手机WebKit）。覆盖首页、URL恢复、三城上限、70城搜索与选择状态、图例、缩放、120个月、数据表、本地状态清除与撤销、加载骨架、请求失败重试、合法零记录空状态、响应式矩阵、触控尺寸与键盘操作。
- Tooltip自动化确认包含统计月份、变动率、原始指数和发布日期；桌面使用鼠标触发，手机使用真实`touchscreen.tap`触发。
- 响应式矩阵在Chromium与WebKit均执行：375×812、390×844、768×1024、1366×768、1440×900、844×390；自动校验页面无水平溢出、图表尺寸稳定且Canvas非空。
- 390×844手机视口自动测量可见关键控件至少44×44像素，图表缩放按钮与图例按钮间距至少8像素；键盘E2E验证筛选、图例和3像素focus-visible焦点环。
- Lighthouse 13.4.0生产构建：Accessibility 100，Performance 92，FCP 2.1秒，LCP 2.4秒，TBT 210毫秒，CLS 0.001；原始报告为`docs/lighthouse.json`。
- 本机正式渠道冒烟：Chrome 149与Edge 147均显示6张城市卡片、页面无水平溢出、图表Canvas为1298×390。
- 最终截图：`docs/screenshots/mobile-390x844.png`、`docs/screenshots/desktop-1440x900.png`。
- URL筛选恢复、旧版本/无效参数提示、最多三城、清单与数据版本、本地筛选清除和撤销均有自动化覆盖。
- 生产发布强制匹配当前完整审计报告；原子切换覆盖成功、首次发布和失败恢复测试。前端逐条验证清单、版本化数据地址、状态、记录结构、唯一键和指数计算。
- 固定分享图为1200×630 PNG，E2E校验Open Graph尺寸、MIME和资源响应。
- 正式构建支持`VITE_PUBLIC_SITE_URL`与`VITE_CONTACT_URL`：模拟HTTPS构建已验证绝对`og:url`、`og:image`、canonical和页脚纠错入口。`release:check`在真实未验项下正确失败，在测试夹具下正确通过。
- 2026-07-16内部预览视觉打磨：按`frontend-design`、`ui-ux-pro-max`和`impeccable`执行；新增内部环境标识和当前比较范围摘要，未加入商业化功能。设计系统记录见`DESIGN.md`和`design-system/70/`。
- 2026-07-16趋势城市选择器：接入完整70城数据；常用六城置顶，其余城市按拼音首字母分组，支持中文、完整拼音和首字母搜索。桌面使用360px搜索浮层，移动端使用底部选择面板；已选项置灰，3城上限禁用其余选项。桌面与移动端E2E均通过`wlmq`搜索并加入乌鲁木齐验证；2026-07-18起允许清空主图。
- 2026-07-16移动端筛选：移除顶部展开按钮，筛选内容改为页面内始终可见的全宽分组；保留URL状态、原生时间选择和44px触控目标。
- 2026-07-16累计变化图：在主趋势图与月份数据表之间增加首月100基准图，按同一住宅类型的月度环比指数复合到最新月份；展示每城最新累计值和相对起点涨跌，数据表同步增加累计值列。明确标注其不是实际元/㎡房价，缺少环比时停止后续计算；第二个ECharts接近视口时懒加载并保留固定高度。
- 2026-07-16累计图回撤提示：Tooltip新增“较高点”，按截至当月的历史最高累计值计算回撤；创新高显示 `0.0%`，不使用未来月份高点。
- 2026-07-17城市选择器：常用城市置顶，其余城市按完整拼音生成A-Z分组并在组内升序排列；隐藏列表行中的拼音，保留中文、完整拼音和首字母搜索。
- 2026-07-17布局与信息层级优化：按 `frontend-design`、`ui-ux-pro-max` 和 `impeccable layout` 执行六项改动。手机端去除重复统计月份并保留发布日期、将筛选压缩为两行、把六城排序口径移到标题下方、将卡片辅助文字提升至12像素；桌面端移除趋势区巨型白色外框；数据状态集中到来源区，页脚仅保留必要入口。主要结构间距收敛到4像素尺度，并增加语义z-index令牌。
- 2026-07-17手机筛选对齐：两行筛选改为两列等宽网格，指标、住宅类型和时间范围三组保持相同宽度；完整住宅分类文案保持单行，补充320、384和390像素视口验证。
- 2026-07-17首页文案与移动排版：主标题改为“读懂你的城市住宅价格变化”，说明文字改用更亲切且准确的环比、同比解释；手机端移除中文段落的 `42ch` 宽度限制，使用完整内容宽度，并补充320、375、384和390像素断言。
- 2026-07-18住宅类型标签：筛选分段控件由官方全称简化为“新房/二手房”，与产品和设计规范统一；移除长标签专用的移动端缩字规则，使住宅类型与指标按钮保持相同字号和间距，数据契约与来源审计仍保留官方术语。
- 2026-07-18第二轮整体优化：发布清单升级为schema 1.1.0，新增六城摘要和70个版本化城市分片；浏览器不再读取17.5MB完整数据文件，主入口脚本由约738KB降至181KB，ECharts拆为摘要就绪后加载的独立块。城市Sheet补齐打开聚焦、移动端焦点圈定、滚动锁定和关闭焦点恢复；70城分组启用`content-visibility`。筛选、城市和本地存储恢复动作拆分作用域；图表拖动与缩放按钮共享状态并显示月份范围；手机精确数据改为按城市分组列表。同步移除重复kicker、提高来源正文可读性并补齐异步状态播报。
- 2026-07-16总体表纠错：发现旧解析器会把无独立标题的面积分类表误认为总体表，旧审计因此检出7060条错误定位；升级解析器为`official-html-v5-exclude-size-band-tables`并全量重解析。实际指标变化6946条，覆盖2018-01至2026-06共102个月，其中新房2229条、二手房4717条；另10694条仅解析器版本或来源定位变化。修订全部追加保留，发布版本更新为`2026-06-611d87e6ba03`。
- 数据审计升级为`full-record-audit-v2`，逐条独立验证新房/二手房表类型及总体/面积分类，旧v1审计报告不再满足发布门禁。
- Playwright固定为2个worker，避免Windows/WebKit并行解析17640条数据和初始化Canvas时产生主线程争抢；完整E2E仍为22项。
- 2026-07-16 Impeccable detector最终复跑：`App.tsx`和`styles.css`返回空结果；最终手机与桌面截图已人工检查，无重叠、裁切或页面水平溢出。

## 发布审查

- Web Interface Guidelines来源：`vercel-labs/web-interface-guidelines` `command.md`，最新获取日期2026-07-18。
- 审查文件：`apps/web/src/App.tsx`、`apps/web/src/TrendChart.tsx`、`apps/web/src/styles.css`、`apps/web/index.html`。
- `apps/web/src/App.tsx:184`：核心筛选作为页面内持续可见的具名区域，不再依赖手机顶部展开按钮。
- `apps/web/src/App.tsx:212`：已选趋势城市使用可访问按钮呈现，并暴露当前选中状态。
- `apps/web/src/App.tsx:221`：70城搜索具有可访问名称，支持中文、完整拼音和首字母输入。
- `apps/web/src/App.tsx:242`、`apps/web/src/App.tsx:246`：显著显示“数据引自国家统计局网站（www.stats.gov.cn）”和非官方网站声明。
- `apps/web/src/TrendChart.tsx:149`、`apps/web/src/TrendChart.tsx:150`：图标缩放按钮有名称和disabled状态。
- `apps/web/src/TrendChart.tsx:146`、`apps/web/src/TrendChart.tsx:153`、`apps/web/src/TrendChart.tsx:162`、`apps/web/src/TrendChart.tsx:166`：缩放控件和图例使用group语义，图例可键盘显隐，月份选择器和等价数据表语义完整。
- `apps/web/src/styles.css:3`、`apps/web/src/styles.css:10`、`apps/web/src/styles.css:14`、`apps/web/src/styles.css:138`、`apps/web/src/styles.css:224`：横向安全区、focus-visible、跳转链接、图表触摸策略和reduced-motion规则存在。
- `apps/web/src/styles.css:94`、`apps/web/src/styles.css:112`、`apps/web/src/styles.css:210`：城市选择器具备稳定布局、清晰焦点状态和移动端底部面板安全区处理。
- `apps/web/index.html:5`：viewport允许缩放并包含安全区；固定SEO和Open Graph元数据位于第7-13行。
- 最终复审：上述文件无剩余Web Interface Guidelines自动可判定违规项。
- 逐项证据矩阵：`docs/ACCEPTANCE_EVIDENCE.md`。

## 尚未验证

- Android微信内置浏览器真机。
- iPhone微信内置浏览器真机。
- 正式Safari当前版及前一稳定版；Windows仅完成WebKit自动化，不能等同正式Safari。
- Chrome、Edge、Safari前一稳定版矩阵；本机只保留当前Chrome和Edge。
- Codex内置浏览器本轮无法附着本地标签；已由独立Chromium/WebKit E2E和截图替代，但不将其等同于微信真机。
- 微信分享卡片在正式HTTPS域名下的实际抓取效果。
- 正式上线主体的转载和商业使用法律确认。
- 正式纠错联系方式。

上述项目未完成前，不把本地Web MVP描述为“已公开上线”。
