# v2.4.2 12 次完整云端回放记录

状态：`passed_limited`（普通月度隔离云端回放已通过；其他发布和外部平台范围仍未完成）。

本文件是 `v2.4.2` 当前候选的完整云端月度自动更新回放记录。候选提交、普通 CI 运行、云端回放运行和报告工件已经绑定完成。

## 必须绑定的身份

- 小程序版本：`v2.4.2`。
- 精确 Git SHA：`94db4dd749f0ff4c0b2fa860e193e202b95f28a1`。
- 普通 CI 运行：`30755560402`，结果 `success`。
- 云端回放运行：`30755834116`，结果 `success`。
- 解析器版本：`official-html-v7-product-housing-only`。
- 审计器版本：`full-record-audit-v5`。

## 必须完成的范围

从 `2025-06` 基线连续模拟到 `2026-06` 目标，执行 12 轮普通月度自动更新。每轮必须记录来源发现、解析、独立审计、候选错误阻断、完整 70 城数据包生成、72 个数据对象加 1 个控制对象的隔离云端回读、受保护指针切换和小程序完整包激活耗时。

每轮必须确认：560 条目标月份记录正确、历史记录没有变化、删除记录和篡改值两类错误候选在上传前失败关闭、切换城市不产生额外下载，并且 `production_pointer_untouched=true`、`production_release_prefix_untouched=true`、`automatic_release_enabled=false`。

## 当前限制

`v2.4.1` 的旧回放报告不能迁移或改名作为本版本证据。当前 `v2.4.2` 只在普通月度隔离云端回放范围内有 `passed_limited` 证据，不得据此宣称开发者工具编译、真机验收、微信审核或正式发布已经完成。

## 云端回放结果（2026-08-02）

- GitHub Actions：[`30755834116`](https://github.com/yilugesanhua/housing-price-index/actions/runs/30755834116)。
- 精确提交：`main@94db4dd`；该提交的小程序唯一机器版本为 `v2.4.2`。
- 工作流结果：`success`；回放报告结果：`passed`；报告 `run_id` 和 `cloud_run_id` 均为 `30755834116-1`。
- 范围：以 `2025-06` 为基线连续回放到 `2026-06`，共 12 轮；目标月份为 `2025-07` 至 `2026-06`。
- 安全断言：`production_pointer_untouched=true`、`production_release_prefix_untouched=true`、`automatic_release_enabled=false`。
- 每轮数据断言：解析 560 条目标月份记录，历史记录变化数为 0；完整审计批次为 126 批、70,560 条记录；解析器为 `official-html-v7-product-housing-only`，审计器为 `full-record-audit-v5`。
- 每轮候选断言：70 个城市分片均生成；删除一条官方记录、篡改一项结构合法官方值两类错误候选均在上传前失败关闭，旧指针字节保持不变。
- 每轮远程与客户端断言：隔离 COS 中 72 个数据对象和 1 个控制对象均完成哈希回读；小程序模拟客户端激活完整 70 城数据包，保留 70 城本地历史，切换城市新增下载次数为 0。

### 12 轮逐阶段耗时

以下是隔离云端演练的实际耗时，不是对未来真实发布日的保证。调度模型为每 5 分钟检查；官方页面出现后正常预期 10 至 25 分钟，内部目标 30 分钟，预警目标 45 分钟；校验失败时保留旧月份且不切换指针。

| 轮次 | 目标月份 | 来源发现和解析 | 失败关闭门禁 | 数据包生成 | 错误候选阻断 | 隔离上传、回读和切换 | 小程序激活 | 总耗时 |
| --- | --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| 1 | 2025-07 | 401 ms | 513 ms | 987 ms | 1,635 ms | 167,161 ms | 1,125 ms | 171,830 ms |
| 2 | 2025-08 | 329 ms | 509 ms | 997 ms | 1,630 ms | 152,733 ms | 756 ms | 156,961 ms |
| 3 | 2025-09 | 330 ms | 510 ms | 1,007 ms | 1,631 ms | 136,905 ms | 755 ms | 141,145 ms |
| 4 | 2025-10 | 322 ms | 549 ms | 1,010 ms | 1,580 ms | 126,805 ms | 743 ms | 131,018 ms |
| 5 | 2025-11 | 341 ms | 522 ms | 1,013 ms | 1,586 ms | 133,543 ms | 787 ms | 137,799 ms |
| 6 | 2025-12 | 336 ms | 510 ms | 1,002 ms | 1,651 ms | 139,130 ms | 800 ms | 143,435 ms |
| 7 | 2026-01 | 286 ms | 527 ms | 1,011 ms | 1,580 ms | 155,663 ms | 786 ms | 159,860 ms |
| 8 | 2026-02 | 339 ms | 537 ms | 1,006 ms | 1,644 ms | 128,774 ms | 789 ms | 133,095 ms |
| 9 | 2026-03 | 345 ms | 543 ms | 1,007 ms | 1,686 ms | 135,340 ms | 794 ms | 139,722 ms |
| 10 | 2026-04 | 335 ms | 540 ms | 1,024 ms | 1,668 ms | 127,546 ms | 799 ms | 131,920 ms |
| 11 | 2026-05 | 334 ms | 551 ms | 1,017 ms | 1,661 ms | 132,165 ms | 796 ms | 136,531 ms |
| 12 | 2026-06 | 326 ms | 556 ms | 1,034 ms | 1,696 ms | 153,163 ms | 793 ms | 157,574 ms |

总耗时为 `1,740,890 ms`（29 分 00.890 秒），每轮平均 145.074 秒，最慢一轮 171.830 秒。按阶段累计：来源发现和解析 4,024 ms；失败关闭门禁 6,367 ms；数据包生成 12,115 ms；错误候选阻断 19,648 ms；隔离上传、回读和切换 1,688,928 ms；小程序激活 9,723 ms。

### 本轮问题记录

- 本轮没有新增失败或阻断问题。
- `REPLAY-PADDING-2025-07` 至 `REPLAY-PADDING-2025-11` 为 5 条信息记录，不是错误：官方来源从 `2016-01` 开始，前五个 120 个月客户端窗口需要 5 至 1 个月的纯 `null` 填充，发布日期为空；覆盖范围内数据和每轮目标月 560 条记录均通过审计。
- `REPLAY-001` 至 `REPLAY-009` 是此前运行中已修复的问题，本轮只保留其追溯记录，没有重新发生。

### 仍未完成的范围

本结果只关闭 `v2.4.2` 普通月度隔离云端回放范围，不代表云函数已部署、微信开发者工具已编译、Android/iPhone 真机已验收、微信已审核或正式版已发布。历史修订专项 12 轮、确定性候选归档、开发者工具、双真机和微信平台证据仍保持未完成。
