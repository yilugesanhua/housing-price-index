# v2.4.2 双真机验收单

> **状态：已完成基础真机确认；18项全量验收仍未完成**
> 本表只适用于绑定后述精确身份的 `v2.4.2` 体验版。未填写、无原始证据或身份不一致的项目均视为未通过。

本地自动化的 `passed_limited` 状态只在 [`IMPLEMENTATION_STATUS.md`](IMPLEMENTATION_STATUS.md) 登记，不替代本表的Android/iPhone操作、原始证据和精确候选身份；本表在两台设备实际执行前继续保持未通过。

## 测试前绑定

| 字段 | 值 |
| --- | --- |
| 精确Git SHA | `3b84e9a293e18fbd960328be243e0e84f18612a1` |
| 微信开发版/体验版版本号 | 本地真机调试 `v2.4.2`；尚未上传开发版或设为体验版 |
| 微信开发者工具版本/基础库 | Stable `v2.01.2510290` / `3.17.0`（Android现场） |
| 包体积/编译证据SHA-256 | 当前运行包 `@antv/wx-f2/index.js`：`00645843974f1f96ac5185c6e979c3c8f234652fdc9a323071e55efa5f91f333`；完整编译证据待补 |
| 不可变候选ZIP/SHA-256 | `小程序源码-v2.4.2.zip` / `9537150768abb6d452ad2fec57bb8c01121f5867426bfc6cb8fb9911e3b1603d` |
| `candidate-manifest.json` SHA-256 | `e08b69d4d97f26ccfa92927bf09403c30bd2002cdf2a42a5a67c0ab04945e547`；两台设备必须绑定同一候选 |
| 内置源数据版本/截止月份 | `2026-06-4fd1d1a8ff12` / `2026-06` |
| 测试远程版本/源版本 | 未现场记录，不得据此宣称远程更新验收通过 |

两台设备必须扫描同一体验版，并与同一不可变候选ZIP及候选清单身份对应。重新上传、重新构建、替换数据或改变上述任一身份后，已完成结果全部失效。稳定归档在双真机通过后由候选原字节晋级，不是测试前置条件。

## 每台设备必测

| 编号 | 操作 | 通过标准 | 证据 |
| --- | --- | --- | --- |
| 1 | 首次在线打开 | 随包内置快照自身校验通过后立即显示首页，控制检查在内部异步执行且不阻塞首屏；后续远程切换只有在控制、回执和完整数据门禁通过后生效 | 截图/网络日志 |
| 2 | 首次离线或清储后离线 | 随包内置快照自身校验通过且未命中客户端当前已知撤销时正常显示；不得仅因没有可信控制状态进入 `unavailable`，联网恢复后再异步刷新控制面 | 录屏 |
| 3 | 不可用状态恢复 | 联网点击重试后，只有完整校验通过才恢复全部数据视图 | 录屏/网络日志 |
| 4 | 允许及拒绝模糊定位 | 允许时按当前规则匹配城市；拒绝后仍可手动选城 | 截图/录屏 |
| 5 | 24小时定位缓存 | 本机仅保存匹配城市与定位时间；过期、未来时间或无效记录在启动读取和点击定位读取时删除 | 录屏/存储检查 |
| 6 | 清除本地记录 | 来源页确认后同时清除筛选、城市来源和定位城市缓存 | 录屏/存储检查 |
| 7 | 手动选城并重启 | 城市列表顺序、选择结果和重启持久化符合当前页面 | 录屏 |
| 8 | 切换住宅、指标、面积和时间 | 当前页面实际存在的数值、统计和图表随筛选同步，无溢出或错位 | 截图 |
| 9 | 滑动70城排名 | 可查看完整排名，当前城市标识与名称清楚 | 录屏 |
| 10 | 操作全部走势图 | 触摸、滑动、日期、图例和曲线对应正确；图表失败文案不指向隐藏入口 | 录屏 |
| 11 | 添加、隐藏、移除趋势城市 | 最多三城且可见状态、按钮和当前页面一致 | 录屏 |
| 12 | 来源、隐私、免责声明和分享 | 用户可见披露与实际模糊定位链路、本地保留边界一致；分享返回后状态不丢失 | 截图/录屏 |
| 13 | 普通新月份更新 | 受信控制与新鲜动态回执通过后，一次下载并校验完整70城再原子切换；回执过期或错绑、下载/校验失败均不改变活动版本 | 网络及页面证据 |
| 14 | 更新后断网切换非常用城市 | 至少三个非常用城市可直接显示，新增下载次数为0 | 录屏/请求日志 |
| 15 | 受审计历史修订 | 修订清单、账本、双重撤销和完整数据均通过后才切换，修正值逐项一致 | 录屏/校验报告 |
| 16 | 损坏、冲突或未批准修订 | 拒绝候选，继续使用未撤销安全版本；不存在安全版本时进入 `unavailable` | 故障注入证据 |
| 17 | 受控回滚早于内置月份 | 仅在控制指针、撤销登记、替代目标和完整包闭环时启用目标；任一不一致失败关闭 | 录屏/控制状态证据 |
| 18 | 弱网、断网、前后台和重新打开 | 半包不激活；每次重新进入前台的 `onShow()` 同步返回、不等待网络，同一会话复用内部刷新并防抖，超时/拒绝由内部收敛；页面无生命周期超时、未处理Promise、空白、裁切、状态串版或水平溢出 | 录屏/请求日志/控制台日志 |

## 设备记录

| 项目 | Android | iPhone |
| --- | --- | --- |
| 机型 | 待填写 | 待填写 |
| 系统版本 | 待填写 | 待填写 |
| 微信版本 | 待填写 | 待填写 |
| 测试人/时间 | 待填写 | 待填写 |
| 18项结果 | 待填写 | 待填写 |
| 原始证据索引及SHA-256 | 待填写 | 待填写 |

## 基础真机确认记录（2026-08-03）

用户确认 Android 与 iPhone 在当前 `v2.4.2` 候选上均可正常使用。Android 现场控制台已显示 `breadth`、`trend`、`cumulative` 三张图表成功初始化；此前 `@babel/runtime` 缺包错误已不再出现。该结论只覆盖“首页可显示、图表可初始化、当前常用操作正常”的基础确认，不替代上表18项的逐项执行、原始证据、远程更新、故障注入、定位/缓存、弱网或回滚验证。

| 项目 | Android | iPhone |
| --- | --- | --- |
| 当前候选基础可用性 | 用户确认正常；首页与三张图表已成功初始化 | 用户确认正常 |
| npm运行包问题复核 | 已重新同步、清编译缓存、重连后不再出现此前 Babel 缺包错误 | 用户确认正常；未单独记录控制台截图 |
| 18项全量结果 | 未逐项填写，保持未通过 | 未逐项填写，保持未通过 |

本节不是“36项全部通过”结论，也不构成上传、审核、发布、远程更新、隐私或回滚验收证据。

## 放行规则

- 两台设备36项必须全部通过，且测试身份与上线审查、交接单和最终上传包完全一致。
- 任一数据身份冲突、错误版本被启用、原子切换失败、隐私披露不实、Canvas空白、关键交互失效或无证据项目均阻断发布。
- 本表通过也不能单独开启生产自动发布；仍须完成上线审查、交接、微信审核发布和正式版读取复核。
