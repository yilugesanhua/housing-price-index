# v2.4.11 近三年普通月度自动更新108次回放

> 状态：`passed_limited`。三组各36个月的本地隔离回放和36个国家统计局正式页面现场复核已经通过；当前未提交工作区尚未完成同版本隔离COS/GitHub Actions、微信开发者工具或Android/iPhone真机验证，因此本报告不是正式云端全链路通过或发布证据。

## 1. 身份与边界

| 项目 | 结果 |
| --- | --- |
| 小程序源码版本 | `v2.4.11` |
| Git执行基线 | `main@d6d5c2870f768e4990ef73a117ac3b6f1e52ec8b`，开始时已有331项工作区改动 |
| 月份范围 | `2023-06 -> 2026-06`，每组36个不同目标月份 |
| 组数/执行数 | 3组，共108次连续月度更新 |
| 模式 | 本地隔离内存对象存储；生产指针、生产发布前缀和生产开关未触碰 |
| 官方源现场复核 | `work/live-historical-source-verification/live-v2411-36-month-official-source-20260804/report.json` |

本轮只修改测试脚本、测试工作流、测试规则和证据文档，没有修改 `apps/miniprogram/` 客户端构件，因此不触发小程序版本递增或开发者工具目录同步。

## 2. 三组结果

| 组 | 报告 | 结果 | 耗时 | 新问题 |
| ---: | --- | --- | ---: | --- |
| 1 | `work/full-auto-update-replay/local-v2411-36x3-run1-attempt-2/report.json` | 36/36通过 | 318,638 ms | 无阻断问题 |
| 2 | `work/full-auto-update-replay/local-v2411-36x3-run2-attempt-1/report.json` | 36/36通过 | 298,116 ms | 无阻断问题 |
| 3 | `work/full-auto-update-replay/local-v2411-36x3-run3-attempt-1/report.json` | 36/36通过 | 299,839 ms | 无阻断问题 |

三组核心流水线合计 `916,593 ms`，约15分17秒。三组逐月的官方归档HTML SHA-256、数据集版本和目标月份全部一致，交叉差异为0。每份机器报告均保留36轮的阶段、耗时、问题编号和逐轮证据。

## 3. 正确性与失败关闭

- 108轮共重解析60,480条目标月记录；每轮恰好70城、每城8种住宅类型/面积口径、560条新增，历史记录变化为0。
- 每轮都构造“删除1条记录”和“结构合法但篡改1个指数值”两类错误，共216个错误候选；全部在上传前被阻断，隔离指针字节和SHA-256保持不变。
- 每轮回读72个数据对象和1个控制对象，共回读7,884个对象；全部哈希通过后才切换隔离指针。
- 每轮小程序客户端从上一月快照激活目标月完整70城数据，共108次激活；切换太原、海口和西宁的新增下载均为0。
- 36个国家统计局正式页面于2026-08-04现场读取成功，页面显示的统计月份和发布日期全部匹配归档；36个实时页面的原始字节SHA-256均与保存归档完全一致，现场重解析20,160条记录并逐条匹配。

## 4. 逐轮提示

三组中每组 `2023-07` 至 `2025-11` 的29轮包含来源覆盖前空值提示，共87条信息项。原因是固定120个月客户端窗口早于项目 `2016-01` 官方来源起点；这些月份只保留四字段均为 `null` 的测试填充，发布日期为空，不属于官方数据，也未进入生产数据。每组从 `2025-12` 起不再需要填充。

除上述信息项外，108轮成功报告的 `issues_detected` 没有阻断问题。

## 5. 本次发现的问题

| 编号 | 发现位置 | 问题 | 修复与复验 |
| --- | --- | --- | --- |
| `REPLAY-012` | 第1组第一次启动 | 外层命令只等待5秒，后台回放完成2轮后被终止，只留下 `running` 检查点，没有最终报告 | 改为独立后台进程、日志重定向和逐月检查点监控；第1组从第1个月重跑并36/36通过 |
| `REPLAY-013` | 首次完整小程序测试 | 云端工作流已为36个月提高到300分钟，但安全测试仍硬编码要求旧的75分钟 | 更新为同时约束12/36月封闭选项、300分钟、精确输入、默认分支和隔离前缀；小程序测试305/305通过 |

`REPLAY-001` 至 `REPLAY-011` 继续作为历史问题保留，不是本次108轮新失败。

## 6. 标准检查与剩余证据

- `npm.cmd run check` 退出码0：Web单元测试18/18、数据测试49/49、小程序测试305/305、发布门禁3/3、70,560条全量数据校验和生产构建通过。
- `npm.cmd run test:e2e` 退出码0：39项通过、1项按既有配置跳过。
- 本机没有腾讯云发布密钥；GitHub受保护密钥只允许默认分支精确提交，而当前 `v2.4.11` 是未提交工作区，默认分支仍为 `v2.4.9`。在取得提交/推送授权并形成当前精确提交前，不能把旧默认分支的云端运行冒充为本版本证据。
- 尚未执行三组隔离COS 36个月回放、微信开发者工具编译和当前候选双真机验证；生产自动发布必须继续保持关闭。
