# v2.4.11 近三年普通月度自动更新 108 次回放

> 状态：`passed_limited`。三组各 36 个月的真实隔离 COS/GitHub Actions 回放已通过；本报告不代表微信开发者工具编译、Android/iPhone 真机验收、微信审核或正式发布已完成。

## 1. 身份与边界

| 项目 | 结果 |
| --- | --- |
| 小程序源码版本 | `v2.4.11` |
| 回放提交 | `main@2bd9e4e354c113f091f1fe7302a54a1c41e2f163` |
| 默认分支 / PR | `origin/main`，PR #1 已 squash 合并 |
| 月份范围 | 基线 `2023-06`，目标 `2023-07 -> 2026-06` |
| 组数 / 执行数 | 3 组，共 108 次连续月度更新 |
| 云端模式 | GitHub Actions + Tencent COS；只写 `housing-data/rehearsals/<run-id>/` |
| 生产安全边界 | 生产 `current.json`、生产发布前缀均未触碰；`AUTOMATIC_RELEASE_ENABLED=false` |

回放只使用 `apps/miniprogram/` 已合并候选的输入，不改变客户端构件。8 个进入小程序构件的源码文件与微信开发者工具目录逐字节一致；`project.config.json` 仅有格式化和末尾换行差异，不影响构件内容。

## 2. 三组结果

| 组 | GitHub Actions 运行 / artifact | 结果 | 耗时 | 新问题 |
| ---: | --- | --- | ---: | --- |
| 1 | [30868946299](https://github.com/yilugesanhua/housing-price-index/actions/runs/30868946299) / `full-auto-update-replay-30868946299-1` | 36/36 月、216/216 阶段通过 | 3,578,078 ms（59.63 分钟） | 无回放阻断问题 |
| 2 | [30872081163](https://github.com/yilugesanhua/housing-price-index/actions/runs/30872081163) / `full-auto-update-replay-30872081163-1` | 36/36 月、216/216 阶段通过 | 4,760,622 ms（79.34 分钟） | 无回放阻断问题 |
| 3 | [30877516940](https://github.com/yilugesanhua/housing-price-index/actions/runs/30877516940) / `full-auto-update-replay-30877516940-1` | 36/36 月、216/216 阶段通过 | 6,891,093 ms（114.85 分钟） | 无回放阻断问题；见 `REPLAY-014` |

三组核心流水线合计 `15,229,793 ms`（约 4 小时 13 分 49 秒）。三次的基线、目标月份和解析/审计版本一致；三次均为 `passed`，没有跨组数据差异。

## 3. 正确性与失败关闭

- 108 次共重解析 `60,480` 条目标月记录；每次恰好 70 城、每城 8 种口径、560 条新增，历史记录变化均为 `0`。
- 每次均注入“删除 1 条记录”和“结构合法但篡改 1 个指数值”两类错误，共 216 个错误候选；全部在上传前阻断，隔离指针字节和 SHA-256 保持不变。
- 每次上传后全量回读 72 个数据对象和 1 个控制对象，共 `2,592` 个数据对象、`36` 个控制对象；全部哈希通过后才切换隔离指针。
- 小程序模拟客户端 108 次从上一月快照激活目标月完整 70 城数据；每次本地城市历史为 70，切换城市下载数均为 `0`。
- 每次官方来源阶段都验证发布日期门禁、官方 URL、归档 HTML SHA-256、560 条解析记录和精确归档匹配；36 个目标月的逐条官方归档审计通过。
- 生产指针、生产发布前缀和生产自动发布开关在三次回放中均未改变。

## 4. 每次回放提示

每组 `2023-07` 至 `2025-11` 的 29 次包含来源覆盖前空值提示，共 87 条信息项。固定 120 个月客户端窗口早于项目 `2016-01` 官方来源起点；这些月份只保留四字段均为 `null` 的测试填充，发布日期为空，不属于官方数据，也未进入生产数据。每组从 `2025-12` 起不再需要填充。

除上述覆盖提示外，三次报告均无数据或流程阻断问题。

## 5. 问题记录

| 编号 | 发现位置 | 当前现状 | 修复 / 后续动作 | 状态 |
| --- | --- | --- | --- | --- |
| `REPLAY-012` | 第 1 组首次启动 | 外层命令只等待 5 秒，后台回放完成 2 轮后被终止，没有最终报告 | 改为独立后台进程、日志重定向和逐月检查点监控；第 1 组从第 1 个月重跑并 36/36 通过 | 已修复 |
| `REPLAY-013` | 首次完整小程序测试 | 工作流上限改为 300 分钟后，安全测试仍要求旧的 75 分钟 | 更新为封闭的 12/36 月输入、300 分钟上限、精确默认分支和隔离前缀；小程序测试 305/305 通过 | 已修复 |
| `REPLAY-014` | 第 3 次 GitHub Actions 收尾注释 | `checkout`、`setup-node`、`upload-artifact`、`download-artifact` 的旧固定 SHA 使用 Node.js 20，GitHub 以 Node.js 24 强制运行并给出弃用提示；本次回放未失败 | 已统一升级到支持 Node.js 24 的官方 Action，并继续固定完整 SHA：`checkout v7.0.1`、`setup-node v7.0.0`、`upload-artifact v7.0.1`、`download-artifact v8.0.1`；需在新提交上通过普通 CI 和回放回归 | 已修复，待回归 |

`REPLAY-001` 至 `REPLAY-011` 继续作为历史问题和修复证据保留，不是本次三组回放的新失败。`REPLAY-PADDING-*` 仅是覆盖范围外的 null 测试填充提示，不是数据错误。

## 6. 标准检查与剩余证据

- 合并提交的普通 CI `30868705225` 通过；本地 `npm.cmd run check` 和 `npm.cmd run test:e2e` 均通过（E2E 39 项通过、1 项按既有配置跳过）。
- 三次真实隔离云端回放均使用同一合并提交，运行结论均为 `success`；每次 artifact 均包含机器可读 `report.json` 和 `issues.json`。
- 小程序源码版本和 8 个构件文件已同步到微信开发者工具目录；本次未执行开发者工具编译、Android/iPhone 双真机验收、云函数生产部署、微信审核或正式发布。
- 生产自动发布继续关闭；三次回放通过不等于生产上线，也不授权写入生产 `current.json`。
