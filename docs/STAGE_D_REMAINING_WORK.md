# 阶段 D 剩余工作清单

更新日期：2026-08-30  
适用候选：`v2.5.27`  
当前结论：本地阶段 D 实现已完成；外部验收和正式发布准入尚未完成。

## 一、已经完成

- 严格发现观察阶段的单日 27 个时段已经通过。无需再次等待连续 3 天、10 次或重新做 27 个时段测试。
- D03-D07 的本地发布身份、来源索引、历史修订账本和哈希校验已实现并通过本地测试。
- D17、D18、D19、D20 的本地代码和故障测试已完成；D19 线上演练已完成，D18 线上复核已运行但发现历史来源变化，后续人工处置仍未完成。
- 本地验证已通过：
  - `npm.cmd run check`
  - `npm.cmd run test:e2e`
  - 小程序/自动化测试 `473/473`
  - Web 端到端测试 `39 passed / 1 skipped`
  - 当前数据校验 `100800` 条
- 当前生产开关仍为：
  - `AUTOMATIC_RELEASE_ENABLED=false`
  - `PRODUCTION_RELEASE_AUTHORIZED=false`
- 本轮未修改小程序页面，未部署、未写入正式数据、未切换正式指针、未上传或发布小程序。

## 二、还需要完成的工作

| 顺序 | 工作 | 由谁操作 | 完成标准 | 当前状态 |
| --- | --- | --- | --- | --- |
| 1 | 建立干净的 `v2.5.27` 候选 | 维护人/Codex | 候选由明确提交生成，ZIP、清单、数据身份和 SHA-256 可回读；当前工作区已有继承脏改动，不能直接把当前目录当作干净候选 | 已完成（隔离候选） |
| 2 | 当前候选同提交 GitHub CI | 维护人/Codex | 默认分支上的普通 CI 成功，候选和 CI 绑定同一精确提交；完成跨环境候选字节复现 | 已完成（PR #6 默认分支 CI；PR #7 修复后 CI） |
| 3 | D18 季度历史页面复核线上演练 | 维护人 | 运行季度工作流；历史页面无变化时生成复核证据；变化或非 HTML 响应时只生成隔离人工任务，不改数据或正式指针 | 已运行但需人工处理（首次 `33298387289`：170个变化、1个请求失败；修复后重跑 `33300833376`：171个变化、0个失败；Artifact 均已下载到项目外） |
| 4 | D18 真实变化后的隔离处置 | 维护人 | 对一条真实或受控变化完成隔离记录、人工判断、证据保留和后续修订入口；不能直接改生产数据 | 已完成数据级复核（171条均为页面变化、业务字段0变化；2013-06重试成功；未生成修订申请） |
| 5 | 配置 D19 故障负责人 | 维护人 | 在 GitHub 仓库 Variables 配置 `HOUSING_DATA_INCIDENT_OWNER`；不得把令牌、密钥或个人敏感信息写进代码、日志或 Artifact | 已完成（`yilugesanhua`） |
| 6 | D19 Issue 生命周期演练 | 维护人 | 真实完成同一故障的 Issue 去重、打开、负责人确认、超时只升级一次、恢复后带 SHA-256 证据关闭 | 已完成（Issue #8 脚本直连；Issue #9 Actions 确认/恢复；修复后月度检查失败回调 `33300899238` 自动打开 Issue #11，Actions `33300990998`/`33300991022` 完成确认和带哈希恢复关闭） |
| 7 | 当前候选微信开发者工具验收 | 维护人 | 导入 `70城小程序技术验证/`，重新编译当前候选，保存编译结果和主包大小；不能用旧版本截图替代 | 未完成 |
| 8 | Android/iPhone 双真机验收 | 维护人 | 使用同一候选记录启动、数据更新、失败回退、弱网/离线和页面关键流程；保存设备、系统、候选身份和结果 | 未完成 |
| 9 | 当前候选云端隔离链路 | 维护人/Codex | 在隔离或受保护环境完成云函数、远程完整数据包、状态部署、回滚、私有审计和发布后监测的真实运行与回读；生产 `current.json` 原字节不变 | 部分完成（运行 `33299529772` 隔离写入/回读/回滚通过，其他链路待补） |
| 10 | 微信平台审核、发布和线上回读 | 维护人 | 仅在前述证据齐全并明确授权后进行；保存平台版本、时间、构件身份和线上实际回读 | 未完成 |
| 11 | 最终自动发布准入评估 | 维护人 | 启用清单所有适用项均有当前证据，逐项复核后由维护人再次明确授权；此前不得开启任何生产开关 | 未完成 |

## 三、GitHub 上需要维护人操作的地方

1. 进入仓库 `yilugesanhua/housing-price-index` 的 **Settings -> Secrets and variables -> Actions -> Variables**。
2. 新增变量名：`HOUSING_DATA_INCIDENT_OWNER`。
3. 值填写故障负责人使用的 GitHub 用户名或团队标识，不要填写令牌、密码或验证码。
4. 保存后运行 D19 工作流的真实演练，并保留运行编号、Issue 编号、确认时间、升级时间和关闭证据。

候选提交、工作流运行和外部平台验收必须绑定同一精确提交；不能只凭“工作流成功”或旧截图认定完成。

## 四、完成顺序

```text
干净候选
  -> 同提交 CI 与跨环境复现
  -> D18/D19 线上演练
  -> 开发者工具重新编译
  -> Android/iPhone 双真机
  -> 云端隔离链路回读
  -> 平台审核/发布与线上回读
  -> 启用清单逐项复核
  -> 维护人再次明确授权后，才评估生产自动发布
```

任一步骤失败都要保留失败原因和最后一个安全版本，不能跳过步骤、重写历史证据或用旧候选替代当前候选。

## 五、明确不需要做的事

- 不需要重新等待连续 3 个完整窗口。
- 不需要把 10 次发现当作新的准入门槛。
- 不需要修改 Web 或小程序页面来完成本阶段。
- 不需要现在把 `AUTOMATIC_RELEASE_ENABLED` 或 `PRODUCTION_RELEASE_AUTHORIZED` 改为 `true`。
- 不需要现在上传、审核、发布小程序或写入正式数据。

## 六、当前阻断结论

在第二节第 1 至第 10 项证据齐全前，阶段 D 只能记为本地实现和 `passed_limited`，不能称为正式自动发布已通过，也不能称为无人值守生产自动化已启用。

相关权威记录：

- [`IMPLEMENTATION_STATUS.md`](IMPLEMENTATION_STATUS.md)
- [`MONTHLY_DATA_AUTOMATION_PLAN.md`](MONTHLY_DATA_AUTOMATION_PLAN.md)
- [`AUTOMATION_ACTIVATION_CHECKLIST.md`](AUTOMATION_ACTIVATION_CHECKLIST.md)
