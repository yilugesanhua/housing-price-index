# 月度数据自动更新执行方案

> 状态：`current_execution_baseline / approved_plan`
> 生效日期：2026-08-26；最近状态复核：2026-08-30
> 适用范围：后续所有月度数据自动更新的设计、修改、演练、部署、运行复核和故障处理。
> 当前实施状态：`strict_discovery_active / production_release_automation_disabled`。本方案不是部署、发布或生产开关已启用的证据。

## 1. 作用与边界

本方案把后续工作统一到一条可执行、可核验的流程上，重点解决“每20分钟发现”不能只停留在Cron表达式的问题。

它不替代下列现有权威文件：

- 官方来源、字段、解析和数据正确性，遵循 [数据契约](DATA_CONTRACT.md)。
- 远程数据、缓存、回滚、候选和权限规则，遵循 [小程序每月自动数据更新](MINIPROGRAM_DATA_UPDATE.md)。
- 生产自动发布的全部硬门槛，只以 [自动更新启用清单](AUTOMATION_ACTIVATION_CHECKLIST.md) 为准。
- 当前实现与验证差距，只在 [实施状态登记](IMPLEMENTATION_STATUS.md) 登记。

发生冲突时，按 `AGENTS.md` 的优先级处理；本方案不得放宽任何数据校验、生产权限或发布门槛。发现任务永远不得直接发布正式数据。

## 2. 严格20分钟发现的定义

具体的北京时间、起止时段和Cron表达式仍只在 `MINIPROGRAM_DATA_UPDATE.md` 维护。本方案要求当前时间表中的27个发现时段都满足以下条件：

1. 每个时段有唯一的 `slot_id`、计划开始时间、实际开始时间、结束时间、结果和重试记录。
2. 首次只读发现必须在计划时段后2分钟内真正开始访问发现逻辑，而不是只创建一个排队任务。
3. 超过2分钟才开始，即使后来成功，也必须记录为 `late`，不能当作准时完成。
4. 一个时段重复触发只能得到同一条时段记录；重复事件不能制造第二次业务处理。
5. 最后一个发现时段到每日截止时间之间只允许完成该时段的有限重试；截止后不再启动新的发现请求，只记录结果和告警。

这里的“严格”是可验证的服务规则，不声称公共云或GitHub能够保证秒级绝对准点。任何迟到、漏跑或官方来源超时都必须留下失败证据，不能被后续成功静默覆盖。

### 2.1 严格发现观察阶段的唯一验收门槛

自2026-08-28起，评估严格发现流程只使用一个完整北京时间日窗口。该窗口内的27个计划发现时段必须全部单次、`succeeded`、`timing_status=on_time`，并逐项与唯一观察对象的`slot_id`、载荷哈希和交接身份一致；`calendar`和`discovery-observation`记录不得计入27次。

同一完整窗口还必须至少一次明确报告`result_status=update_available`，且该报告绑定可追溯的官方新增月份和URL。同一月份的重复报告只证明持续发现，不算多份新数据。第27个时段完成后，必须导出当天记录并运行完整27时段只读审计；只有`expected_slot_count`、`received_slot_count`、`unique_slot_count`均为27且`errors=[]`，才可登记为通过并评估下一阶段。

任一迟到、失败、重复、重试遗留、缺记录或身份不一致，都必须先定位并修复，再从新的完整日窗口重新计数；这条“重新计数”只适用于**首次验收尚未通过**的候选。单个完整日窗口一旦通过，后续日期的27时段仅作阶段C日常健康复核：即使某天健康复核失败，也不得撤销、重置或延长已通过的测试阶段门槛，只需保留失败证据并执行最小修复。连续3个完整发现窗口、连续10个计划发现时段、部分窗口或多日累计都不是现行门槛，只能作为历史运行记录。

## 3. 目标流程

```text
同步官方日程
  -> 时段控制器
  -> 只读发现
  -> 不可变观察报告
  -> 候选准备与数据门禁
  -> 受保护发布
  -> 发布后守卫与回滚
```

### 3.1 同步官方日程

- 日程只决定何时检查，不是正式数据来源。
- 月度与年度官方日程必须继续交叉核对；冲突、缺失或无法验证时停止并报告。
- 日程同步与正式页面发现职责分开，不能把日程同步任务误算为一次发现完成。

### 3.2 时段控制器

- 腾讯云定时器按分钟唤醒控制器；控制器按北京时间计算当前是否到达应执行的20分钟时段。
- 控制器在CloudBase数据库中以事务领取时段，并使用可过期租约、防重和重试次数，避免并发或中断后永久占用时段。
- 时段状态至少区分：`pending`、`started`、`succeeded`、`retrying`、`failed`、`late`、`expired`。领取成功不等于发现已经开始或已经成功。
- `monthlyDataWatchdog` 现已是严格时段的独立控制器和只读发现执行器：它按北京时间领取时段、直接完成发现并写入观察记录。GitHub仅保留为审计和备用信号，不再被当作准时发现的依据。此处替换部署前“只检查GitHub运行”的历史描述，不改变20分钟时段、权限边界或生产发布关闭状态。

### 3.3 只读发现

- 腾讯云只读发现执行器是严格时段的主执行者，直接运行与GitHub相同的官方来源白名单、日程判断和月份比较逻辑。
- 发现逻辑必须由单一、可测试的共享模块提供，不能维护两套会逐渐不同的页面识别规则。
- 当前正式月份只能以生产云端 `current.json` 的只读回读为准；仓库内Web清单、历史报告或待发布状态只能交叉核验。
- 每次发现生成非敏感的观察报告。发现新月份只产生固定身份的交接，不写正式数据、不改变正式指针、不更新小程序页面。
- 每次成功观察还必须写入按 `sha256(slot_id)` 固定命名的非业务观察对象；GitHub 只能通过现有只读监测身份读取该对象，不能读取 CloudBase 数据库或申请生产写权限。

### 3.4 GitHub的职责

- GitHub保留为代码审计、可复现报告、人工重放和备用信号，不再作为严格时段的唯一时钟。
- GitHub定时任务迟到、漏投或排队时，必须由时段控制器记录并处理；晚到的GitHub运行不能冲掉已记录的 `late` 或 `failed` 状态。
- 任何备用补发仍使用同一 `slot_id` 和同一发现身份，避免形成触发风暴。
- GitHub 候选门禁必须同时验证腾讯云观察对象的 `observation_id`、`payload_sha256`、`timing_status`、指针月份、官方 URL、幂等键和 `handoff_identity`。缺失、迟到、失败、过期或哈希冲突时必须失败关闭；GitHub 自己的 `handoff.json` 不能替代腾讯云证据。

### 3.5 候选、发布和监测

- 发现后才允许进入候选准备；候选必须使用稳定的发布身份、不可变原始来源和可恢复状态机。
- 相同官方原文、代码提交和配置必须得到相同候选字节。检查时间、运行ID和重试次数只能进入审计字段，不能改变候选身份。
- 候选、发布、回滚和正常状态部署继续遵循现有独立门禁。任一门禁失败时，正式 `current.json` 保持原字节不变。启用清单 I09 的一次受保护状态演练是唯一有限例外：默认分支、当前成功发现运行、固定确认词和两个开关均为`false`时，才允许仅改变状态字段和控制代次；数据、来源、清单、撤销身份及数据转换类型必须保持不变并写后回读，不能把演练当作发布或自动化已启用。
- 发布后监测只读回读正式指针、清单、完整70城数据和安全回退状态；问题进入告警或人工隔离，不能通过重跑绕过门禁。
- 待发布恢复必须重新读取并核对同一腾讯云观察对象，不能只相信仓库中持久化的候选字段；对象身份变化或不存在时停止恢复。

## 4. 网络、重试与故障处理

1. 官方日程、发布列表和正式页面正文的连接、响应头、正文读取、长度校验和哈希计算必须属于同一次尝试。
2. 每次请求都有明确超时；确认前一次请求已终止后才可以重试，避免重叠访问和重复处理。
3. 重试只在本时段允许的时间内进行，并使用同一 `slot_id`。达到截止时间仍失败时标记为 `failed` 或 `expired`，等待人工处理或下一个合法时段。
4. 官方页面返回验证码、拦截页、结构变化、来源冲突、缺表、缺城或月份冲突时，按数据契约失败关闭，不能把错误页面当作官方数据。
5. GitHub、CloudBase数据库、只读生产指针或网络任一依赖不可用时，保留最后一个安全数据版本，写入原因和时间；绝不降级为无校验发布。
6. D19已完成一次线上受控通知闭环演练；在其余启用清单门槛未全部关闭且两个生产开关仍为`false`时，现有运行摘要和失败状态仍只能作为有限告警信号，不得称为无人值守自动化已启用。

## 5. 权限与安全边界

- 发现执行器只拥有访问官方公开页面、写自身时段记录/非业务观察对象和读取已授权生产指针所需的最小权限；没有生产数据写权限。GitHub 只读身份只能读取观察对象和既有正式只读对象。
- 看门狗的GitHub令牌只允许读取Actions运行并在需要时触发固定发现工作流；不得拥有Contents、Secrets、Variables、Administration、Deployments或生产云端权限。
- 令牌、密钥、验证码和正式数据写凭据不得写入源码、文档、日志、Artifact或聊天。
- `AUTOMATIC_RELEASE_ENABLED` 必须保持 `false`，`PRODUCTION_RELEASE_AUTHORIZED` 必须保持 `false` 或未设置，直到启用清单全部适用项有当前可复查通过证据且维护人再次明确授权。
- 只读发现、重试、看门狗、日程同步和观察报告均不得借任何例外写正式数据或开启发布。

## 6. 实施顺序

### 阶段A：本地实现与测试

1. 提取共享的只读发现模块，供GitHub和腾讯云执行器共同使用。
2. 建立时段记录、租约、幂等领取和失败恢复状态机。
3. 将日程同步、时段控制、发现执行和GitHub审计拆分为清晰职责。
4. 建立 CloudBase 观察对象的确定性命名、写后回读和 GitHub 只读消费门禁。
5. 保留现有数据门禁和生产双开关，不在本阶段部署或打开任何生产写入能力。

### 阶段B：隔离云端演练

1. 在不含生产写凭据的云函数中部署时段控制器和只读发现执行器。
2. 先以dry-run记录所有时段，不触发备用GitHub工作流。
3. 模拟定时漏投、GitHub排队、请求超时、数据库领取失败和重复事件，验证时段不会丢失、重复或永久锁死。
4. 进入下一阶段评估前，必须按[第2.1节](#21-严格发现观察阶段的唯一验收门槛)完成一个完整日窗口的严格发现观察和只读审计。

### 阶段C：启用只读保障

1. 取得对云函数和定时器的明确部署授权后，才将经过演练的只读补救从dry-run切为实际执行。
2. 现场回读定时器、时区、函数版本、最小权限和最新运行记录，确认严格时段规则已生效。
3. GitHub仍仅作为审计和备用重放；这一阶段不等于自动发布已启用。

### 阶段D：发布能力的独立评估

只有在自动更新启用清单全部适用项通过、隔离验证和外部平台证据完成、两个生产开关分别被维护人明确授权后，才可单独评估正式自动发布。严格发现通过不能替代发布、回滚、真机或正式数据链路验收。

## 7. 每次变更必须遵循的规则

1. 先读取本方案、数据契约、数据更新规范、启用清单和实施状态，再确定修改范围。
2. 修改时段、时区、Cron、看门狗、发现逻辑、候选身份、解析器、门禁、云函数或工作流时，必须同步更新对应测试和本方案涉及的事实说明。
3. 修改 `apps/miniprogram/` 中会进入客户端构件的云函数源码、运行时校验、配置或内置数据时，必须按版本规范递增 `apps/miniprogram/config/version.js`，并同步到微信开发者工具目录；只改不进入客户端构件的云端脚本或文档时不触发版本递增。
4. 任何部署、令牌配置、定时器变更、生产开关修改、正式数据写入、回滚、上传或发布，都需要该次明确授权和现场回读；方案本身不构成授权。
5. 不得通过缩短时段、放宽超时、删减测试、重写历史证据或把历史成功记录当成当前通过来关闭问题。
6. 改变本方案本身时，必须同时说明变更原因、影响范围、替代或废止的规则、测试要求和生效日期；与权威文件冲突时先更新权威文件，不得仅修改本方案。

## 8. 验收与证据

严格20分钟发现的实现候选至少应提供以下当前证据：

- 覆盖全部27个时段的本地时区和时段计算测试，以及跨日、重复触发、租约过期和截止时间测试。
- GitHub漏投、排队和晚到时，CloudBase记录和处理结果的故障注入测试。
- 官方日程、发布列表和正文读取的完整重试与失败关闭测试。
- 同一官方原文多次触发只得到一个稳定发现身份、一个候选身份和可复查恢复路径的测试。
- GitHub 读取腾讯云观察对象、对象缺失/哈希冲突/迟到、月份或官方 URL 不一致时失败关闭的测试；候选和待发布恢复都必须绑定同一 `slot_id` 与观察身份。
- 符合[第2.1节](#21-严格发现观察阶段的唯一验收门槛)的完整日窗口只读审计、函数和时区现场回读，以及不含生产写入的权限核验。
- 任何自动更新代码改动仍须按启用清单和数据更新规范完成适用的隔离回放、CI、数据校验、构建、开发者工具、真机和正式数据链路证据；不得用本方案替代这些门槛。

只有当前证据同时证明“按时开始”“正确完成”“失败可见”“没有生产越权”时，才能把严格发现记为已实现。否则应如实登记为 `partial`、`failed` 或 `not_tested`。

## 9. 当前结论与后续维护

### 2026-08-30 阶段D当前状态

当前源码版本为 `v2.5.28`。本轮已补齐发布能力评估前的本地安全能力：历史修订在远端下载、临时缓存校验、缓存回读、发布候选复用、状态部署、发布后监测、人工/自动回滚和预览验证时都必须携带同一份已校验字节长度与 SHA-256 的修订清单；回滚到历史修订包也不得跳过该校验。D18季度历史页面复核和D19故障通知均已完成一次线上受控闭环，且未写入正式数据、修改生产指针或开启生产开关。维护人另行确认 `v2.5.28` 已完成 Android/iPhone 双真机、微信审核和正式发布；这不等于每月数据自动发布已启用。

### 2026-08-30 当前代码完整隔离回放与正式数据只读回读

- 完整隔离回放曾因每轮候选缺少范围化发布审计身份而失败关闭。修复提交 `79db8e264ab5753791b6c3f90163afa00e79258f` 已合并为默认分支 `e322e19d031b86934ee90309da6ffc8c0d5f3128`；默认分支 CI `33315713592` 成功。
- 该提交的 GitHub Actions `33315927833` 成功完成当前普通月度单组连续6个月回放（`2026-01` 至 `2026-06`）。工件报告为 `status=passed`、`replay_count=6`，每轮 `publication_identity_verified=true`，且 `production_pointer_untouched=true`、`production_release_prefix_untouched=true`、`automatic_release_enabled=false`。这符合本方案和启用清单对普通月度6个月隔离验证的当前范围要求。
- 随后的只读监测 `33316750559` 成功回读正式 `2026-06-f80465ae29a5`、云函数和全部70个城市分片，报告仍为 `production_pointer_untouched=true`。两道生产开关经独立回读仍精确为 `false`；本轮没有写正式数据、修改正式指针、上传或发布小程序。
- 该证据只补齐当前普通月度隔离回放和一次正式数据只读回读，不替代候选身份、历史修订专项、平台原始记录、其他启用清单项或维护人未来对双开关的明确授权。自动数据更新继续保持 `deployed / passed_limited`，不得据此开启自动发布。

本轮同时完成 D03-D07 的本地信任链补强：候选业务记录、完整独立审计报告、来源索引和精确提交身份在发布前重新计算并逐项核对；历史修订账本仅允许保留旧前缀并追加批准差异，`release_type` 与 `reason_type` 分离，业务字段哈希不受运行时元数据影响，最新月份来源批次与历史修订来源批次分别精确绑定。相关定向测试 `29/29`，完整小程序/自动化测试 `473/473`，项目级 `npm.cmd run check` 和 `npm.cmd run test:e2e` 均通过。本地证据状态为 `passed_limited`，不代表当前候选已完成 CI、云端、开发者工具、真机、平台审核、发布或生产启用。

### 2026-08-30 D17/D20本地补齐（未部署）

- D17 的 `scripts/data/status.ts` 现在按年度保存官方法定工作日配置元数据。2026年配置绑定国务院办公厅2025年11月4日通知（`china-state-council-holidays-2026-v1`）；未配置年份返回 `waiting_for_official_calendar`，跨年工作日截止日返回空值并保持 `updating`，只暂停未知年份的SLA截止日，不猜测节假日。定向测试覆盖已知调休、未知年份、跨年边界和状态判定。
- D20 的 `scripts/miniprogram/cross-platform-oracle.mjs` 绑定当前市场/城市文件及小程序快照 SHA-256，固定144个筛选组合（两种房屋类型、四种面积、两种指标、三种时间范围、三座焦点城市）。独立期望结果分别与Web核心和小程序页面逐值比较排名、涨跌计数、层级/省内排名和累计变化，并单独覆盖缺失值、并列排名。该工具和测试只用于本地一致性证据，不改变页面或迁移共享逻辑。
- 本次补齐仍只具备本地 `passed_limited` 证据；D17需在官方发布下一年度配置后更新并现场复核，D20需当前候选同提交CI、开发者工具和双真机证据。阶段D的外部前提、自动发布双开关和生产数据边界不变。

本轮仍不满足第6节阶段D的完整生产准入前提：D18/D19已完成有限范围线上证据，`v2.5.28` 的双真机、微信审核和发布也已由维护人确认；但当前版本仍缺不可变候选、同提交CI、平台线上回读和正式数据链路证据，启用清单其余项目也尚未全部完成。严格发现观察结论与生产自动发布的关闭状态均不因本轮改变。

截至2026-08-29，严格发现器源码、共享发现契约、时段状态机和 CloudBase 观察对象到 GitHub 的消费门禁已完成本地验证；当前工作区候选版本为 `v2.5.25`。`monthlyDataWatchdog` 已于2026-08-26 23:02（北京时间）用本轮源码重新部署，CloudBase 函数列表回读为 `Deployment completed`，手动调用返回只读 `strict_status=idle` 且 GitHub 补发路径关闭。该调用发生在当日窗口之后，不能作为完整窗口或准时发现证据。GitHub 只读监测身份已安全配置；现场验证证明它可读取正式 `current.json`，对不存在的观察对象得到 `NoSuchKey`，对正式发布目录得到 `AccessDenied`。2026-08-27的完整审计已满足[第2.1节](#21-严格发现观察阶段的唯一验收门槛)；严格发现的观察阶段为`passed`，自动数据更新整体仍为`deployed / passed_limited`。2026-08-29截至北京时间11:37的8个业务时段均首次、准时、成功并报告官方`2026-07`可更新；前八个时段只读审计使用`--slot-count=8`得到`8/8/8, errors=[]`。该快照和当天其余日常发现时段只作为持续运行健康证据，不重新构成阶段验收，也不延迟下一阶段评估；17:55后的完整导出如执行，也只登记为当日健康审计。严格发现通过仍不能替代自动发布启用清单、发布、回滚、真机或正式数据链路验收。

### 2026-08-29 16:38 实时发现健康复核

截至北京时间16:35，CloudBase `monthlyDataWatchdog` 当天前23个业务发现时段均首次、准时、成功；实时只读审计使用`--slot-count=23`得到`23/23/23, errors=[]`，并逐条回读观察对象验证载荷哈希和交接身份。该记录仅作为当天持续运行健康证据，不替代17:55后对完整27个时段的日终审计，也不改变已通过的单日27时段观察门槛或生产发布关闭状态。

### 2026-08-29 17:17 实时发现健康复核

截至北京时间16:55，CloudBase `monthlyDataWatchdog` 当天前25个业务发现时段均首次、准时、成功；实时只读审计使用`--slot-count=25`得到`25/25/25, errors=[]`，并逐条回读观察对象验证载荷哈希和交接身份。该记录仅作为当天持续运行健康证据，不构成新的测试准入条件，不改变已通过的单日27时段观察门槛或生产发布关闭状态。

### 2026-08-29 17:56 日常完整窗口健康复核

第27个时段完成后，CloudBase `monthlyDataWatchdog` 当天完整27个业务发现时段通过严格只读审计：`--slot-count=27`得到`27/27/27, errors=[]`；27个 COS 不可变观察对象逐项回读并与时段记录核对，`checked_count=27`、`passed_count=27`、`errors=0`。这只是阶段C日常健康证据，不重新构成测试准入条件，也不改变已通过的单日27时段观察门槛或生产发布关闭状态。

17:59（北京时间）最终只读回读仍确认正式指针为`2026-06-f80465ae29a5`，仓库和生产环境发布开关均为`false`；未发生正式数据写入或小程序发布。

### 2026-08-29 阶段C只读保障令牌生成、预检与配置（当前）

阶段C的专用配置脚本 `scripts/miniprogram/configure-watchdog-readonly-audit.mjs` 已通过本地安全测试。维护人确认生成后，无过期日期的最小权限正式令牌 `housing-watchdog-2026-permanent` 已安全保存到本机环境；GitHub Actions API只读预检返回HTTP `200`，随后执行`--apply`并收到 CloudBase 请求编号 `6d9a8cc9-02b0-42a3-9540-3f867b3d104d`。现场回读确认函数为 `Active`、唯一每分钟触发器仍在，5个固定变量已配置，备用审计路径为`WATCHDOG_GITHUB_AUDIT_ENABLED=true`、`WATCHDOG_DRY_RUN=false`。此前到期的 `housing-watchdog-2026-rotated-2` 与 `housing-watchdog-2026-rotated-3` 已在新令牌验证成功后撤销。令牌不写入仓库、文档、日志或聊天，配置脚本不拥有正式数据写入路径；本次不构成自动发布授权。

该令牌按设计只有目标仓库 `Actions: Read and write` 与 `Metadata: Read-only`，读取仓库变量和生产 Environment 变量接口返回HTTP `403`。因此两个生产开关不能由本令牌复读，必须使用独立授权身份；生产指针已独立只读回读且原字节未变。非敏感证据保存在项目外 `C:\Users\user\CodexAuditEvidence\permanent-watchdog-token-20260829\readonly-recheck.json`。

### 2026-08-28 严格发现观察门槛变更

维护人曾要求将阶段B的有限观察门槛由“连续3个完整发现窗口”缩短为“连续10个计划发现时段”。该决定已被同日下方的`supersedes`决定取代，只作为历史记录保留。

### 2026-08-28 单日27时段观察门槛（supersedes 10时段规则）

维护人明确决定：后续评估发现流程使用[第2.1节](#21-严格发现观察阶段的唯一验收门槛)的单个完整日窗口，不要求连续三天完整窗口，也不以10个时段作为最终门槛。该决定自2026-08-28起持续有效；后续只有带日期、明确写出`supersedes`的维护人决定，同时更新本方案、实施状态和相关审计说明后，才可再次调整。

为避免规则冲突，本方案的第2.1节是严格发现观察门槛的唯一规则来源；`IMPLEMENTATION_STATUS.md`只能登记某次运行是否达到该门槛，`MONTHLY_DATA_WATCHDOG.md`只能说明执行和审计方法，启用清单只能登记其外部证据状态。历史的10时段、连续3窗口或任何部分窗口记录均不得被这些文件重新表述为有效门槛。

后续每次月度自动更新工作都必须先按本方案判断当前阶段和授权范围，再执行对应步骤。发生新的运行异常、数据源变化或云平台规则变化时，先保留证据、停止危险操作，并在不放宽安全边界的前提下更新本方案及对应权威规范。
